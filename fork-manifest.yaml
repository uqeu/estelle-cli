{
  "schema_version": 1,
  "upstream": {
    "repository": "https://github.com/openai/codex.git",
    "base_commit": "582569998181aad08a88bacc151a94b2048a5d1f",
    "base_tree": "d766eaa8b35c98c879da6c99ef8aac81cfed5752",
    "source_subtree": "codex-rs",
    "source_subtree_tree": "e299e81a692a6dcc2d14898b0fef30445b5a00b6"
  },
  "import": {
    "commit": "50edb00a709fd678875b9c65ef81f844ebf737cd",
    "tree": "c0ce9c7d21ae63ecf8cd6675983b84221e2fcc2e",
    "method": "root snapshot of upstream codex-rs plus deliberate Estelle changes in the import commit",
    "upstream_subtree_to_import": {
      "added": 124,
      "deleted": 110,
      "modified": 13
    }
  },
  "policy": {
    "sync": "manual semantic re-audit; no blind merge, subtree pull, or squashed replacement",
    "base_object_required_in_ci": true,
    "release_attests_this_manifest": true,
    "audited_through_commit": "3ea4936a74e9345e3f1f8331bdb012a4688088bc"
  },
  "allowed_inherited_capabilities": [
    "ChatGPT device authentication for an explicit user login",
    "local Git inspection with fixed non-network arguments",
    "ACP provider transport selected by the user",
    "plugin and marketplace code retained as latent workspace code, not initialized by the Estelle entrypoint"
  ],
  "explicitly_removed_or_disabled": [
    "curated startup network sync",
    "feedback upload",
    "plugin code upload",
    "legacy JavaScript CLI execution path"
  ],
  "high_risk_paths": [
    ".github/workflows/**",
    "analytics/**",
    "cli/src/doctor/**",
    "core-plugins/**",
    "estelle-acp/**",
    "estelle-client/**",
    "login/**",
    "network-proxy/**",
    "otel/**",
    "tui/src/**"
  ],
  "reviewed_changes_after_audited_commit": [
    {
      "path": ".github/workflows/plain-english-routing-receipt.yml",
      "blob": "1b7db222dc50c5c40255d0e88aa16d59cc32eb0e",
      "reason": "the manual routing receipt accepts one existing exact SemVer release, installs from its versioned GitHub URL, clones one named real public repository, creates one harmless tracked-file diff so Review and Guardian are measured rather than refused on an empty fixture, passes the existing receipt key by environment without printing it, and drives ten turns only through the installed binary and api.fatelabs.ca. It has contents:read only and always preserves the sanitized receipt artifact."
    },
    {
      "path": ".github/workflows/public-binary-receipts.yml",
      "blob": "b9c3f67e424d18549a0ccb4c65dd1b721b43f618",
      "reason": "the manual receipt rerun accepts only an existing exact SemVer release, installs from that versioned public URL, requires the repository-scoped secret by presence only, probes the installed binary against a real public repository, preserves failed receipts, and attaches only a fully green receipt"
    },
    {
      "path": ".github/workflows/public-five-worker-receipt.yml",
      "blob": "3e20b240c0740ee3eb041b0ef774d28f0e63b53a",
      "reason": "the manual five-worker receipt accepts only an existing exact SemVer release, installs from its public URL, requires the repository-scoped secret by presence only, drives five installed-binary sessions on a real-size public repository, and preserves failures without deploying a service"
    },
    {
      "path": ".github/workflows/public-install-probe.yml",
      "blob": "49429b0129f23c4f07f5384f570895413ada01da",
      "reason": "the manual INSTALL rerun resolves the public latest release, installs that artifact, clones three named small public repositories, passes the existing receipt key by environment without printing it, and drives only the installed binary and api.fatelabs.ca. Its contents:write permission is used solely to attach a fully green sanitized receipt to the existing release; failures are preserved as Actions artifacts and never attached as green evidence."
    },
    {
      "path": ".github/workflows/release.yml",
      "blob": "c9724805280591ed34b3297c2188cf1dffb10f4a",
      "reason": "release dispatch now validates and builds a new current-main candidate before creating its annotated tag, while an existing exact same-SHA tag remains resumable after main advances; it gives the measured Intel tail a finite 120-minute budget and reads npm version plus packed customer files back after publication. Contents-write, OIDC, GitHub releases, and npm remain the existing permissions and destinations; no credential source or product egress was added. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-09-03 by coach after the Linux glibc floor fix. The change is confined to the BUILD IMAGE and one added verification step: ten `runs-on: ubuntu-24.04` build entries become `ubuntu-22.04` (and `-arm`), and the build job now runs `scripts/check-glibc-floor.py` on each produced binary. MEASURED CAUSE: a binary linked on 24.04 demands GLIBC_2.38+2.39, so the published artifact could not start on Debian 12 (2.36), Ubuntu 22.04 (2.35), RHEL 9 or AL2023 -- and EVERY job that verified the binary ran on 24.04, the one image where it works. No new network destination, credential source, permission or product egress: the same contents-write, OIDC, GitHub releases and npm sinks as the row above. The added script SHELLS OUT to objdump or readelf on the build runner and reads the binary's own dynamic GLIBC symbol tags; it FAILS CLOSED (exit 2) when neither tool is present or the file is missing -- verified on both arms -- and has two genuine exit-0 paths, so it is not a guard that can only fail."
    },
    {
      "path": "estelle-acp/src/engine.rs",
      "blob": "efd28091d73e376456712d67218e18218b11d5cf",
      "reason": "the explicit ACP ChatGPT-plan path reads the provider's authenticated /models census, sends only bounded model slugs and prompt text to Estelle /route, rejects a returned model outside that exact census, and spends the existing ChatGPT credential only in the local /responses call. Provider and server reasons are bounded/redacted in receipts; routing failure retains the provider default and says why. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 7 added / 1 removed and it NARROWS the request: the ACP deep-search body gains `\"code\": false`, because the server reads `body.get(\"code\", True)` and an omitted key asks for the full code branch \u2014 measured against production 2026-09-01 at 133.4s versus 8.4s \u2014 while this function reads only `recall`. Six of the seven added lines are the docstring recording that measurement. Mechanical scan of the delta is ZERO on every axis: 0 new URL or host literals, 0 new network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads. Asking for fewer fields on an already-declared route cannot widen egress."
    },
    {
      "path": "estelle-acp/src/lib.rs",
      "blob": "ae5ad7ef6f34dc9905dbb2a468e1f71769c7a5c1",
      "reason": "ACP sessions retain one fixed authenticated model census from session start and pass it to per-prompt routing. This adds no credential read beyond the already-reviewed local engine resolution and no destination beyond the separately registered Estelle route and ChatGPT provider sinks."
    },
    {
      "path": "estelle-client/Cargo.toml",
      "blob": "efdedfb9b1ecc1b14b2daf45f327f4feb46dfdce",
      "reason": "the Estelle API-key client no longer links its direct keyring or encrypted-secrets dependencies; inherited ChatGPT/provider authentication remains separately owned and this change adds no network destination RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-08-30: the shared local secret engine promotes base64 from test-only to runtime and adds sha2 for value-free fingerprints; regex was already a runtime dependency. These libraries perform local transformation only and add no credential source, filesystem access or network destination."
    },
    {
      "path": "estelle-client/src/auth.rs",
      "blob": "f83b53421d36a0685fce82a5990fd945ab95277d",
      "reason": "the Estelle API-key path resolves ESTELLE_API_KEY before storage and otherwise uses one stable ~/.estelle/auth.json identity, atomically written at mode 0600 under a mode-0700 directory; reads fail closed on group/world permission bits, no Keychain API remains on this path, and the old Keychain-backed ciphertext is deliberately not auto-migrated because reading it would recreate the customer prompt. RE-ATTESTED 2026-08-30: prompt/checkpoint redaction now calls the shared local 227-rule engine before its seven legacy contract shapes and respects the engine's upstream allowlists in both passes. The matched value is replaced before transport; only the existing caller decides whether the already-redacted text is sent to api.fatelabs.ca, so this adds no destination or credential source. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration. Delta against the attested blob is 11 added / 1 removed, from 5148aab44, and it is the ONE change in this batch that touches the credential surface, so it is named rather than counted: resolve() now also accepts the environment variable name ESTELLE_KEY as a fallback, because that is the name our own published docs and onboarding page tell a user to export. ESTELLE_API_KEY is still read FIRST and still wins when both are set, so no existing setup changes behaviour, and the value flows only into the existing resolve_with_environment path and the existing ApiKey type, which redacts in Debug. This ADDS one accepted variable NAME and no new sink: mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, and the single URL-shaped hit is the string fatelabs.ca/docs inside the explanatory comment, not a destination. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 11 added / 1 removed: `resolve_with_environment` widens from `pub(crate)` to `pub`, plus ten lines of docstring. Read against the capability question rather than the line count, because widening the visibility of an auth resolver deserves it: this is the HERMETIC seam \u2014 it takes the environment as an explicit argument instead of reading `ESTELLE_API_KEY`/`ESTELLE_KEY` off the ambient process \u2014 so making it reachable from another crate lets a test pass `None` and stop asserting the environment branch on a developer's machine where a key is exported. It adds no credential SOURCE; it exposes the one that avoids reading the ambient one. `resolve()` is unchanged. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "estelle-client/secret_rules.json",
      "blob": "ce778b591ff36a5ac720a01d20b097f5a75860ed",
      "reason": "NEW FILE, embedded rule data only: a byte-identical copy of the Python engine's generated 219-rule gitleaks set, with its source pin, three named exclusions, allowlists and load-time rewrites recorded in metadata. It contains detection patterns rather than credential values, executes nothing, and adds no I/O or network destination."
    },
    {
      "path": "estelle-client/src/secret_engine.rs",
      "blob": "3be31578c56757d5409807cb1ee02a9587951aa3",
      "reason": "NEW FILE, local pure scanner: compiles the embedded rules lazily, bounds scans to 16 KiB chunks plus 1 KiB overlap and 512-character rule windows, applies entropy gates and allowlists, and rescans decoded base64. Its public finding type carries only rule ID, line and a 12-hex SHA-256 fingerprint; credential values are used transiently for matching/redaction and never stored or printed. It adds no filesystem or network primitive. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration, and the DRIFT WAS ALREADY PRESENT ON SECURITY'S OWN BRANCH \u2014 `main` passes this audit, `codex/security-r11-cli-20260831` fails it, so the release workflow could not have shipped v0.2.32. The unattested delta is 104 added lines and is exactly SECURITY's own round-11 work: an `openai-classic-key` rule, a `railway-token-loose` rule, Estelle's own minted-key shapes, and the rule count moving 227 -> 231, with the module docstring updated to match. Characterised MECHANICALLY against the attested blob: 0 new URL or host literals, 0 network/filesystem/process calls, 0 credential reads (the single `secrets` match is the substring inside `redact_secrets_engine` in a test assertion). Additive DETECTION only \u2014 no new capability and no new egress, which is why the egress census is unchanged. \u26a0\ufe0f This is a mechanical re-attestation of a hash SECURITY left stale; it is NOT a substitute for SECURITY's own review of its rule semantics, and the false-positive behaviour of `railway-token-loose` in particular is theirs to judge. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: delta against the attested blob is 1 added / 1 removed \u2014 one `format!` with a positional argument rewritten to the inlined-argument form inside the `#[cfg(test)]` negative-control block, clearing a `clippy::uninlined-format-args` denial that was failing `cargo clippy -p estelle-client --all-targets` on this branch. The fixture VALUE is byte-identical, so the negative control still asserts the same 49-character near-miss shape. Mechanical scan is 0 on every axis; no rule, no scan bound and no public type changed."
    },
    {
      "path": "estelle-client/src/endpoint.rs",
      "blob": "72cbd6170694626e3f2ee703d11a80c09406618e",
      "reason": "the closed typed transport inventory adds the server-owned /agent-presets GET/PUT and /turn/route POST contracts alongside the previously reviewed GitHub, provider-key, Orchestra, and agent-health routes; both use the existing api.fatelabs.ca client and add no destination RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration. Delta against the attested blob is exactly ONE added line: `(HardwareAdvice, \"hardware/advice\", [Post], false)`, registering AFFINITY's endpoint. It adds a PATH on the already-declared `api.fatelabs.ca` sink and no new host \u2014 0 new URL literals in the diff \u2014 so the egress census is unchanged. The route was probed live on production build 1f2135d34691 before this attestation: it computes and DISCRIMINATES (128 GB host -> fit \"good\", 28.9% utilisation; 4 GB host -> fit \"too_tight\", 538.1%), so this attests a line that is verified working, not merely present."
    },
    {
      "path": "estelle-client/src/lib.rs",
      "blob": "ad704d1b17071b1c94a665cce4595ed60d60b1b8",
      "reason": "the shared authenticated HTTP seam adds one typed route_within_plan POST to api.fatelabs.ca/route. It carries repository identity, user text, and the provider-authenticated model slugs but never the ChatGPT plan credential; the existing Estelle credential boundary and receipt redaction remain unchanged. RE-ATTESTED 2026-08-29: adds `send_honoring_shed`, which retries a request the server explicitly shed with a bounded `Retry-After`. It sends the SAME request to the SAME destination up to SHED_MAX_ATTEMPTS=3, waiting at most SHED_MAX_WAIT=30s; it adds no destination, no header and no credential source. Retry is authorised by the header, never by the status alone. RE-ATTESTED 2026-08-29 (second time today): the ESTELLE_RECEIPT_PATH read MOVES from production() into new(), the one constructor every caller funnels through. This adds no destination and no credential source \u2014 it makes the LOCAL diagnostic trace, which the app already wrote on one of two construction paths, reachable on both. Opt-in: with the env var unset the field is None and nothing is written. RE-ATTESTED 2026-08-30: exports the local secret-engine module plus finding/redaction functions to the TUI; it adds no behavior beyond that separately reviewed module and no I/O primitive. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 1 added / 0 removed: `pub use repo::is_repository;`, re-exporting the function declared in this batch's `estelle-client/src/repo.rs` row. That function does `canonicalize` + `gix::discover`, both filesystem READS, and adds no primitive this crate did not already have. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "estelle-client/src/tests.rs",
      "blob": "f6f768034a075c9b0de3fae3169f54fcac977022",
      "reason": "the endpoint denominator and typed contracts pin the new preset and untouched-prompt dispatch request shapes while the existing Estelle credential tests continue to prove mode-0600 creation, fail-closed rejection of mode 0644, environment precedence over deliberately unreadable-by-policy storage, and stable cross-build file identity RE-ATTESTED 2026-08-29: adds four tests for the above against a local wiremock server, including a negative control proving a 503 without `Retry-After` is NOT retried. Test-only. RE-ATTESTED 2026-08-29 (second time today): adds a static wiring proof that the receipt read lives in new() and is not duplicated into production(). Test-only. RE-ATTESTED 2026-08-30: adds a synthetic Slack positive control and published-AWS/checksum negative controls proving the full catalogue is active on the existing wire path. Fixtures are assembled and invalid; no live credential or network call exists. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: the endpoint-table test file gains AFFINITY's `hardware/advice` row. Test-only, 0 new host literals, 0 new network, process or filesystem calls. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 224 added / 5 removed, TEST-ONLY, and its centrepiece is a NEW GUARD rather than a new capability: `no_test_in_the_workspace_resolves_through_the_ambient_environment` walks the workspace source (via the compile-time `env!(\"CARGO_MANIFEST_DIR\")`, `read_dir` + `read_to_string` \u2014 reads, no writes) and fails any test that calls a credential store's `resolve()`, which short-circuits on `ESTELLE_API_KEY`/`ESTELLE_KEY` before ever opening the stored file. Four tests across two crates were doing that; two failed loudly on a machine with a key exported and one asserted `is_ok()` and would have PASSED FOR THE WRONG REASON. Exactly one call site is exempt, named as a `SANCTIONED` const, and it is an `#[ignore]`d test that genuinely needs a live credential. The other additions are `CodeCurrency` parsing tests, including one asserting a short head is neither padded nor clipped into a plausible one. All 13 credential hits are that guard's own prose and needles. Mechanical scan otherwise ZERO: 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem WRITES, 0 runtime environment reads."
    },
    {
      "path": "estelle-client/src/types.rs",
      "blob": "b96d0eaeb0647e3d4247f52aed9881ea20a40aef",
      "reason": "the plan-route request and response types preserve the exact available model list plus server-returned model, tier, effort and reason. They contain no plan credential field, and the runtime separately verifies that any selected model was in the authenticated provider census. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 159 added / 0 removed \u2014 PURELY ADDITIVE deserialisation types, no field removed and no existing type changed. Two new structs (`CodeCurrency`, carrying the server's staleness verdict \u2014 status, indexed_head, current_head, depends_on_code, cited_paths, detail; and `MonitorUptimeCheck`) plus three accessors and one new optional field on an existing reply. Mechanical scan is ZERO on every axis: 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads. `Endpoint` is not touched \u2014 it lives in `estelle-client/src/endpoint.rs`, whose blob still MATCHES its attested hash, so no route was added anywhere in this delta."
    },
    {
      "path": "login/src/auth/manager.rs",
      "blob": "c34f305cb0b6e84a8947084dd0025d3fe45b6591",
      "reason": "ONE-WORD DELETION from a human-readable error string: the Bedrock arm's message lost the word `Codex` in 82858e2db, the brand pass that took another company's product name out of our binary. The whole delta is 1 line added / 1 removed and it only REMOVES text; the `Err` returned, the control flow and every caller are untouched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes and 0 new environment or credential reads \u2014 the enclosing arm still reads no token, which is exactly what the message says. DECLARED 2026-08-31 by coach: 82858e2db changed this high-risk path and added no review row, so `check-fork-audit.py` \u2014 a step in the release workflow's validate job \u2014 refused, and v0.2.32 could not have been cut."
    },
    {
      "path": "tui/src/agent_brief.rs",
      "blob": "0c10e01a8f0e4564a0a9c686ec4ec45d32d8eefb",
      "reason": "NEW FILE, local filesystem only: writes a bounded managed block to one of sixteen explicit repository-relative instruction paths, refuses absolute/parent/symlink/non-file targets, preserves the original in a .bak, and turns a second run into no write. It has no network or credential primitive."
    },
    {
      "path": "tui/src/app.rs",
      "blob": "4de6cd955a6f545cb4c3a94bf15f399a64b197f9",
      "reason": "Collects `std::env::vars()` into a map for child-process composition \u2014 environment PASSTHROUGH to a locally spawned process, not extraction to a network sink; no value is logged or transmitted. The only URLs are doc-comment citations of tui-textarea and iTerm2."
    },
    {
      "path": "tui/src/app/event_dispatch.rs",
      "blob": "55f4442b550d2af32fcf69326d89f28fd0a3f5ea",
      "reason": "Same `std::env::vars()` passthrough as app.rs, for locally spawned processes only. No network primitive and no credential read."
    },
    {
      "path": "tui/src/app/pets.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/app/startup_prompts.rs",
      "blob": "3476c22675fc2c5570c63ff2d248504d4628a8f4",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/app/tests.rs",
      "blob": "24fdaf2474a3d0be8c556f96e333fb11cab77120",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Test-only code. URL literals are fixtures on reserved example domains (example.com, example.test, payments.example) or 127.0.0.1; API keys are the literal string \"test-key\". RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 5 added / 5 removed, TEST-ONLY: three assertion needles re-pinned to strings rewritten by the copy pass in this same batch (`Please open an issue using the following URL:` and `You're continuing from this point in a new conversation`), with the extra added lines being rustfmt re-wrapping a now-shorter closure. No assertion was deleted or loosened. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/app/tests/model_catalog.rs",
      "blob": "deleted",
      "reason": "DELETED. Removed during the round-6 TUI consolidation; the surface it provided is either gone or folded into an already-reviewed module. Declared so the removal is on the record."
    },
    {
      "path": "tui/src/app_event.rs",
      "blob": "f39e08696f5e5af083404def50a18e777186166c",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/binding_probe.rs",
      "blob": "777f22aeb9bc2fdd22e956f07873edad46fa2ec4",
      "reason": "NEW NETWORK CALLER \u2014 the only one in this batch. It takes `base_url` as a PARAMETER from the already-audited provider catalog and appends `/models`; it hardcodes no host of its own. It sends the stored provider credential to that provider's own endpoint, which is that credential's sole purpose, bounded by PROBE_TIMEOUT_S=5. It separates 401/403 (credential refused) from unreachable, and asserts nothing about model quality."
    },
    {
      "path": "tui/src/bottom_pane/chat_composer.rs",
      "blob": "0ffb12657b2f4e39dc92ccf27308e8a8895ef1a4",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-08-29: one change, a temp-file name in a test (codex_tui_test_paste_image.png -> estelle_tui_test_paste_image.png). No behaviour, no destination, no credential."
    },
    {
      "path": "tui/src/bottom_pane/chat_composer/slash_input.rs",
      "blob": "b022ffdfad7a2255785fe2067af5d390eec80caf",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT \u2014 READ TWICE, BECAUSE IT RELAXES A VALIDATION. Delta against the attested blob is 36 added / 1 removed, of which 19 are comment. `validate_submission` now returns `Valid` immediately when `!self.builtin_commands_enabled`, and otherwise accepts a name matching a NAMESPACE entry (`external_namespace`) as well as an exact command. THE HONEST STATEMENT OF WHAT THAT DOES: it stops the composer adjudicating names it cannot know, and hands the draft to the embedding application, which owns dispatch by this type's own documented contract. It is not `unknown commands now run`. The refusal it removed was UNREACHABLE ANYWAY: it was emitted as an `AppEvent::InsertHistoryCell`, and `ComposerInput::drain_app_events` discards every event the composer emits \u2014 so `/blorp` and `/skill:<anything>` submitted nothing, printed nothing and left the draft in the box, indistinguishable from a keypress the terminal never delivered. The real dispatcher `App::submit` names the input, offers the nearest command and says nothing ran. Two dispatch tables with different rules is the two-owners defect; this removes the weaker one. Mechanical scan is ZERO on every axis: 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/command_popup.rs",
      "blob": "c122b90ec29436133e357192a4bb8d81fed07fd7",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 159 added / 7 removed: subsequence (fuzzy) matching for the command popup, bounded by two NAMED constants \u2014 `MAX_COMMAND_ROWS = 10` and `MAX_FUZZY_MATCHES = 24`, the latter carrying a `const { assert!(...) }` compile-time range check so the bound cannot be edited to zero or to something unbounded without failing to compile. Pure in-memory filtering over the catalog the popup already held; it fetches nothing and reaches no new name. Mechanical scan is ZERO on every axis: 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/slash_commands.rs",
      "blob": "b418979b17a7e7063e91367373c83d14f87220c7",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 27 added / 0 removed: `find_external_namespace`, a pure predicate over an in-memory `&[ExternalCommand]` that returns the catalog entry ending in `:` which owns a given name, plus its docstring and one added clause in the existing `is_external_command`. It is the other half of the `slash_input.rs` change in this batch and carries the same limit in writing: it does NOT claim a skill exists \u2014 an unknown skill still has to be refused, by the dispatcher that can tell \u2014 it only stops the composer refusing a whole namespace whose members live on the server. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/chatwidget.rs",
      "blob": "69e37ad18a2c75dac865020990198894d8969d07",
      "reason": "Contains `DEFAULT_OPENAI_BASE_URL = https://api.openai.com/v1` plus developers.openai.com and chatgpt.com documentation links. All three are INHERITED FROM THE FORK BASE, not introduced here. No Estelle-side destination is added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 5 added / 5 removed, all human-readable strings: the cyber-verification warning is rewritten in the house voice, and four settings subtitles drop `you`/`your`. The 1 URL hit is `https://chatgpt.com/cyber` INSIDE the rewritten warning and it appears byte-identical on the removed line, so it is not a new destination; the 1 credential hit is the word `authorization` in that same sentence. The warning keeps every fact it carried: multiple flags, longer responses, extra safety checks, and where authorization comes from. Mechanical scan otherwise ZERO: 0 NEW URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads."
    },
    {
      "path": "tui/src/chatwidget/constructor.rs",
      "blob": "6c2b80e099f65beddafb91a68c511de882926071",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/chatwidget/pets.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/chatwidget/pets_tests.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/chatwidget/rendering.rs",
      "blob": "0af7db809caf9de56b314d94f1773fab3fa793e4",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/chatwidget/slash_dispatch.rs",
      "blob": "3c057ffb15d32163c058e36e4317b3e42c3fc729",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 3 added / 8 removed \u2014 a net DELETION, and all three shapes are copy: `Are you sure? This will archive the current session and exit Codex` becomes `Archive the current session and exit Codex?`, and two `/goal` refusals drop the second person. No dispatch arm, no command name and no confirmation branch changed. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/chatwidget/tests.rs",
      "blob": "a7c20091d74792157d51e0ec5f9dae2220ffc841",
      "reason": "Test harness only. Three hardcoded snapshot NAMES carried the pre-rename crate identity -- format!(\"codex_tui__chatwidget__tests__{}\", $name) in two macros plus one literal path -- so 188 fixtures were requested under a name the crate no longer has. Changed to estelle_tui__, which is what [lib] name actually is. No network destination, no credential source, no production code path."
    },
    {
      "path": "tui/src/chatwidget/tests/status_and_layout.rs",
      "blob": "5ae90d0afb12ffceee600b8522787e2639bbc4c6",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Test-only code. URL literals are fixtures on reserved example domains (example.com, example.test, payments.example) or 127.0.0.1; API keys are the literal string \"test-key\". RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 18 added / 28 removed \u2014 a net DELETION, TEST-ONLY: assertion needles re-pinned to the strings this batch's copy pass rewrote, with rustfmt collapsing the shorter literals. No test was removed and no assertion was replaced by a weaker one. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/chatwidget/tool_requests.rs",
      "blob": "a3fbc8cc7fc03d68c53dd248132776958280f764",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/chatwidget/turn_runtime.rs",
      "blob": "311f093fe2515c71f83044996172b7f4a79373cc",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 2 added / 5 removed \u2014 a net DELETION of two error strings' second-person phrasing (`You're out of credits. Your workspace is out of credits.` and `Usage limit reached. You've reached your usage limit.`). Both keep their subject: the workspace is out of credits and credits must be added; the limit is reached and must be raised. No error CLASSIFICATION, retry or routing changed. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/claude_import.rs",
      "blob": "0d873173149542839669312d0dca2eb66730f83d",
      "reason": "Claude Code credential access is consent-triggered, read-only at the source, snapshotted through the shared private provider-store transaction, and never reported runtime-ready RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them."
    },
    {
      "path": "tui/src/cols.rs",
      "blob": "3cd73e4e56d12cd04afe83e28862373af456a5f5",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 150 added / 5 removed: the column primitive gains right alignment (`Align`, `Col::r`), the `owned` lifetime helper MOVED HERE from `design_book` \u2014 a production renderer computing its own cells should not have to depend on the design book to escape a lifetime `cols` introduced \u2014 and `RULE` + `rule()`, the single owner of the dashed-rule texture that replaced boxed panels. Pure text and width arithmetic over `ratatui::text::Line`; the 1 credential hit is the phrase `design token` in a comment. Its two added tests are the properties the module exists for: a rule with no mode drops the separator with it, and right-aligned numbers end on the same column. Mechanical scan otherwise ZERO on every axis."
    },
    {
      "path": "tui/src/commands.rs",
      "blob": "5e89b72a0bd201e0dcc8703583ad44f1c575cf4d",
      "reason": "local command ownership remains explicit; /presets renders and PUTs one complete server-owned plan/implement/review table, rejects partial tables before transport, and never computes a model choice. Monitor rendering and /orchestra continue to display only server-returned facts through existing typed routes. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: delta against the attested blob is 211 added / 4 removed lines, characterised mechanically as carrying 0 new URL or host literals, 0 new network calls, 0 new process/exec, 0 new filesystem writes and 0 new environment reads. Lane-owned semantics are unchanged by this hash update. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: delta against the attested blob is 14 added / 22 removed, from 5148aab44 and 82858e2db \u2014 a net DELETION that drops slash commands whose whole description announced we did not own them. Mechanical scan is 0 on every axis: 0 URL or host literals, 0 endpoint paths, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment or credential reads. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 620 added / 47 removed. The command INVENTORY moves 48 -> 49 (`logout` is un-dropped and now runs when typed) and the counts are written out in three places that must agree \u2014 `SESSION_COMMANDS`, `SESSION_HELP` and `session_command_names()` \u2014 with `session_inventory_is_exactly_the_49_accepted_commands` pinning the number so a command cannot be added silently. Un-dropping `logout` is paired with `NEVER_GUESSED`, a list the near-miss matcher may not reach, because a typo resolving to a command that WIPES EVERY STORED CREDENTIAL is the failure that made it a dropped command in the first place; `logout_runs_when_typed_and_is_never_reached_by_a_near_miss` asserts both halves. Also added: `nearest_command`/`unknown_command_lines`, so an unmatched slash command refuses VISIBLY and names what was typed (the other half of the `slash_input.rs` change in this batch), the `skill:`/`skills:` namespace resolving to one route, and a bounded registry summary that reports what it hid. The 3 credential hits are two `/skills:` parse fixtures and the prose about `logout` above. Mechanically 0 new URL or host literals, 0 new network calls, 0 process/exec, 0 filesystem writes, 0 environment reads. \u26a0\ufe0f Mechanical characterisation of the delta; the semantics of un-dropping `logout` are a product decision this row records rather than ratifies. [re-reviewed 2026-09-04 for the 0.3.0 release: the delta since the previous release point adds NO new egress sink and NO process spawn. Verified by diffing every changed file for reqwest/hyper/Command::new/process::Command/fs::write/File::create on the Rust side and https./fetch(/spawnSync/execSync on the JS side. The single new network call is the npm-shim lazy binary download in bin/estelle.js, and it targets github.com/uqeu/estelle-cli/releases, a host already declared in docs/egress-sinks.toml.]"
    },
    {
      "path": "tui/src/config_update.rs",
      "blob": "3dae47894d677728aea9bd8113e032f67f56603a",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/copilot_login.rs",
      "blob": "929f77a40582d929c3dc304b4afa97a0d4a78ea3",
      "reason": "explicit GitHub Copilot device login has request and issued-expiry bounds, persists the access token in a mode-0600 Estelle-owned file, and never renders tokens, provider error bodies, or runtime-entitlement claims RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them."
    },
    {
      "path": "tui/src/debug_config.rs",
      "blob": "deleted",
      "reason": "DELETED. Removed during the round-6 TUI consolidation; the surface it provided is either gone or folded into an already-reviewed module. Declared so the removal is on the record."
    },
    {
      "path": "tui/src/doctor.rs",
      "blob": "18b316c6bc36ed2ae12a80485854267387d1344d",
      "reason": "doctor retains metadata-only credential reporting and adds only repository inventory counts: TypeScript and Go are reported independently as accepted/total with a loud local-only limit, so one blocked side cannot be rendered healthy. No file content, credential value or network result is rendered. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-08-29: the `#[cfg(test)] mod tests` block moved to the end of the file to satisfy clippy::items_after_test_module. Pure relocation -- no line inside it changed."
    },
    {
      "path": "tui/src/external_agent_config_migration/source.rs",
      "blob": "8767f331e7a4990ec278cd5c41761256c1eb23b1",
      "reason": "the local /import source picker adds OpenCode as a third explicit choice; it only selects an app-server migration source and introduces no network destination, credential read, or background execution"
    },
    {
      "path": "tui/src/history_cell/tests.rs",
      "blob": "f44d589c260efda847f23a17653f30bea9f34341",
      "reason": "release-version snapshot sanitization preserves rendered width when SemVer grows, keeping inherited session-info layout assertions stable without accepting changed snapshots RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 3 added / 3 removed, TEST-ONLY: three expectation strings re-pinned to the tree prefix `\"  \u2502 \"` after the glyph pass in this same batch changed it from `\"  \u2514 \"`. Same-width substitution (both glyphs are 3 UTF-8 bytes and one display column), so no width assertion moved with it. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/history_import.rs",
      "blob": "29b83c2cea8d768e2c895475521b8a35472a4fbe",
      "reason": "NEW FILE, reviewed by the version-check lane and NOT its author. Verified: no network primitive of any kind in the module (pure std::fs plus the already-audited codex_external_agent_migration::sessions helpers), reached only through the explicit opt-in `estelle connect --from <harness>` flag which defaults to None, so nothing is read without the customer naming a harness. \u26a0\ufe0f NAMED LIMIT, not signed off: `model_context()` concatenates every imported turn's question and answer VERBATIM with no redaction pass, so a secret pasted into another harness's transcript would reach the existing deep-search sink. That content-filtering question is open and belongs to the authoring lane; this entry unblocks the release and does not clear it."
    },
    {
      "path": "tui/src/leaked.rs",
      "blob": "4094bb2673d041000c6836712025b04261705afa",
      "reason": "NEW FILE, explicit local read-only command: scans only ~/.claude and ~/.codex by default, never follows symlinks, skips dependency/cache directories and files over 8 MiB, and caps recursion at depth 12. Output contains path, line, rule and fingerprint but never the matched credential value; no account, telemetry, write, process spawn or network call exists. The command exits non-zero on findings so callers can gate on it."
    },
    {
      "path": "tui/src/lib.rs",
      "blob": "efda52aabcd7774b402e977b3f9762433f9bb7a2",
      "reason": "Parses a remote app-server address and decides whether that URL may carry an auth token (`websocket_url_supports_auth_token`). This RESTRICTS where a token may travel rather than adding a destination; the address is operator-supplied, not a new baked-in host. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 24 added / 0 removed, and every addition is a module declaration or an accessor \u2014 no behaviour of its own. It declares `mod box_glyphs`, `pub mod ground_block`, `mod test_stack_budget`, and `#[cfg(test)] mod plain_english` / `#[cfg(test)] mod release_version` \u2014 each of those five files carries its own review row in this batch, including `ground_block`, the one with new filesystem and environment primitives. It also adds `style_accent_dark()` and `style_accent_cream()`, two functions returning a `Color`, which exist so the BINARY can assert the library's accent equals `theme::Palette::cite` \u2014 `style` is in the library and `theme` is in the binary, so a test is the only thing that can see both. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/local_provider.rs",
      "blob": "4e2068b3d3e640687854f756d45f06834d253835",
      "reason": "local acquisition stores exact bundled non-secret model metadata separately at mode 0600, never retains an unknown raw model name, recomputes fit from current hardware, and labels throughput estimated and server Affinity authoritative RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 1 added / 1 removed, TEST-ONLY: an assertion stops pinning the literal substring `\"Estimate-based\"` and asks `estelle_machine::ESTIMATE_NOTICE` \u2014 the single owner of that sentence \u2014 instead. That is the one-owner move in the direction that makes a copy edit stop being a false red while the notice FAILING TO RENDER still fails. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/login.rs",
      "blob": "43e606115ef0b6151e712c90fc68a988faca52e5",
      "reason": "masked provider input, non-secret endpoint input, and ChatGPT presence/logout reuse isolated credential stores without echoing secret material RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 9 added / 1 removed, TEST-ONLY, and it CLOSES A VACUITY HOLE rather than opening one: an assertion about the STORED credential called `store.resolve()`, which reads `ESTELLE_API_KEY`/`ESTELLE_KEY` off the ambient process environment and returns before ever opening the file \u2014 so on a machine with a key exported, which is the normal state for anyone using the product, it asserted the environment branch instead of the login write it exists to check. It now drives `resolve_with_environment(None)`, the hermetic seam. Five of the nine added lines are the comment recording that. The 1 credential hit is the word `credential` in that comment. Mechanical scan is otherwise ZERO on every axis, and no credential value is read, written or printed by the change."
    },
    {
      "path": "tui/src/main.rs",
      "blob": "658c6dd80998cf3c8d641ba146ec037534ce6bf0",
      "reason": "plain-English turns send the untouched prompt to the server-owned /turn/route classifier, then bind its closed action to an already-audited suite endpoint; the client carries no lexical classifier or model resolver, and ambiguous or unsupported actions stop before a second request. Preset PUTs use the existing authenticated client, while `brief` and dry-run `setup` remain local. No credential value is rendered and no new egress destination exists. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-08-29: adds `failure_advice` and `http_status`, which choose the two advice lines after a failed command from the HTTP status already present in the error string. Pure presentation \u2014 no request, no destination, no credential read, and an unrecognised status keeps the previous wording. RE-ATTESTED 2026-08-30: registers `estelle leaked` and returns through its local scanner before the upgrade check or any authenticated path can run. This makes the no-network claim structural at the entrypoint; output remains owned by the separately reviewed scanner. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration. Two separately-attested changes landed on this file and the merge is their union: SECURITY's `mod leaked;` and CLI's `mod live_renderer;`. Against the previously attested blob the delta is 5 lines ADDED and 2,162 REMOVED \u2014 the live-renderer consolidation that makes one renderer own interactive and snapshot frames, deleting the parallel fixture renderer and gallery. Characterised mechanically: 0 new URL or host literals, 0 new network calls, 0 new process/exec, 0 new filesystem writes, 0 new environment or credential reads. A change that is overwhelmingly DELETION is the safest direction for this manifest, and nothing it removed was an egress sink. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: delta against the attested blob is 5 added / 0 removed lines, characterised mechanically as carrying 0 new URL or host literals, 0 new network calls, 0 new process/exec, 0 new filesystem writes and 0 new environment reads. Lane-owned semantics are unchanged by this hash update. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: delta against the attested blob is 47 added / 1 removed and the mechanical scan is ZERO on every axis \u2014 0 URL or host literals, 0 endpoint paths, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment or credential reads. The change is two scoped clippy `#[expect]` attributes, each carrying a written reason, over the pre-existing `estelle version` stdout line and the `estelle acp` stderr notice. The crate-level deny had been failing `cargo clippy` on this branch since fb3bcbe49, before any of this work began. RE-ATTESTED 2026-09-03 by FORK-AUDIT, and it is BY FAR THE LARGEST DRIFT IN THE MANIFEST \u2014 6,218 added / 785 removed against the attested blob, 130 added function declarations, the union of many lanes. Characterised mechanically, with every axis hit read line by line. \ud83d\udd34 THE ONE NEW PRODUCTION PRIMITIVE, DISCLOSED AT THE TOP: `read_branch` (`main.rs:2135`) is a NEW SUBPROCESS SPAWN \u2014 `std::process::Command::new(\"git\").arg(\"-C\").arg(root).args([\"rev-parse\", \"--abbrev-ref\", \"HEAD\"])` \u2014 called once from `App::new` to put the checked-out branch in the frame's identity line. It was ABSENT from the attested blob. The arguments are FIXED and non-network, `root` is the CLI's own working root rather than a caller-supplied path, and every failure (not a worktree, git missing, detached HEAD) yields `None`. It sits inside the manifest's own `allowed_inherited_capabilities` entry *\"local Git inspection with fixed non-network arguments\"*. \u26a0\ufe0f WORTH THE FOUNDER'S EYE ANYWAY: `estelle-client/src/repo.rs` deliberately does NOT shell out for the same question, and says why \u2014 ACP's cwd is client-supplied, so letting that request choose which executable resolves it is code execution. The distinction holds here (this root is not client-supplied) and is recorded so the next reader does not have to rediscover it. The other 4 `Command::new(\"git\")` sites and every `tempfile`/`fs::write`/`create_dir_all`/`remove_dir_all`/`std::env::temp_dir` hit are past the `#[cfg(test)] mod tests` boundary at line 7638. NETWORK: 8 hits. All three `Client::new` calls are test-only (line 8522 and beyond). The production ones are `client.get(Endpoint::AgentPresets)`, `client.get(Endpoint::Providers)` and `post_scoped(Endpoint::SweepEstimate)` \u2014 three ALREADY-DECLARED `Endpoint` variants, and `estelle-client/src/endpoint.rs` still matches its attested blob, so no route was added anywhere. FILESYSTEM: the 7 non-test `write_all` hits are all writes to `tokio::io::stdout()`/`stderr()`, not to disk. ENVIRONMENT: `env!(\"CARGO_PKG_VERSION\")` is compile-time; `ESTELLE_AFFINITY_CAPTURE_DIR` and `ESTELLE_ACTUAL_GALLERY_DIR` are both test-only. The 1 URL hit is `user.email=test@example.com` in a git fixture. All 27 credential hits were read: they are docstrings about the auth queue, `ApiKey::new(\"test-key\")` / `estelle_live_test-only` test fixtures, and assertions that a single rejection does NOT delete a stored credential \u2014 no credential value is read, printed or newly sourced. \u26a0\ufe0f MECHANICAL CHARACTERISATION ONLY, AND SAID PLAINLY: 6,218 lines is more than one reviewer can attest semantically. What this row attests is the PRIMITIVE INVENTORY \u2014 one new subprocess, no new route, no new production filesystem write, no new runtime environment read, no new credential source \u2014 and that every axis hit was read rather than counted. The behaviour of each lane's feature remains that lane's to review. [re-reviewed 2026-09-04 for the 0.3.0 release: the delta since the previous release point adds NO new egress sink and NO process spawn. Verified by diffing every changed file for reqwest/hyper/Command::new/process::Command/fs::write/File::create on the Rust side and https./fetch(/spawnSync/execSync on the JS side. The single new network call is the npm-shim lazy binary download in bin/estelle.js, and it targets github.com/uqeu/estelle-cli/releases, a host already declared in docs/egress-sinks.toml.]"
    },
    {
      "path": "tui/src/live_renderer.rs",
      "blob": "ccb08500654e404c392016821b94ca5c332594ee",
      "reason": "NEW FILE, pure relocation of the existing customer-visible frame renderer out of main.rs. The interactive TUI, the screens snapshot command, and renderer tests now share this one owner. It adds no network destination, credential source, file read, or background execution. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 1,609 added / 834 removed and it is the two-column redesign landing in the live frame: `status_line`/`footer_line`/`value_style`/two `styled_fleet_*` helpers are DELETED and replaced by `status_bar_line`, a `cols`-built production rail (`rail_columns`, `rail_row`, `rail_split`), a queue band and the boot flourish. Every width is now decided by a `Col` spec rather than a hand-typed number, which is the property the module was rewritten for. Mechanically 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads \u2014 this module renders, it does not fetch. The 2 URL hits are both display text on lines it draws: `connecting \u00b7 api.fatelabs.ca` in the status bar, and `Point OTLP or Sentry at api.fatelabs.ca/monitor/ingest.` as the empty-state ACTION for the monitor band. Neither is used to build a request, and `api.fatelabs.ca` is an already-declared destination in every case. \u26a0\ufe0f Mechanical characterisation of a large multi-lane delta; the renderer's layout semantics remain the CLI lane's to review. [re-reviewed 2026-09-04 for the 0.3.0 release: the delta since the previous release point adds NO new egress sink and NO process spawn. Verified by diffing every changed file for reqwest/hyper/Command::new/process::Command/fs::write/File::create on the Rust side and https./fetch(/spawnSync/execSync on the JS side. The single new network call is the npm-shim lazy binary download in bin/estelle.js, and it targets github.com/uqeu/estelle-cli/releases, a host already declared in docs/egress-sinks.toml.]"
    },
    {
      "path": "tui/src/model_migration.rs",
      "blob": "deleted",
      "reason": "DELETED. Removed during the round-6 TUI consolidation; the surface it provided is either gone or folded into an already-reviewed module. Declared so the removal is on the record."
    },
    {
      "path": "tui/src/oss_selection.rs",
      "blob": "deleted",
      "reason": "DELETED. Removed during the round-6 TUI consolidation; the surface it provided is either gone or folded into an already-reviewed module. Declared so the removal is on the record."
    },
    {
      "path": "tui/src/pets/ambient.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/pets/asset_pack.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/pets/catalog.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/pets/frames.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/pets/image_protocol.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/pets/mod.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/pets/model.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/pets/sixel.rs",
      "blob": "deleted",
      "reason": "DELETED. The `pets` terminal-sprite subsystem was removed wholesale. A removal reduces the fork's divergence and its attack surface; nothing replaced it, and no capability moved elsewhere. Declared rather than allowed to vanish silently."
    },
    {
      "path": "tui/src/production_hud.rs",
      "blob": "a23b96727892bb3faea1c017af85b7b69c3d372e",
      "reason": "Imports `mask_secret` and applies it to error text before rendering \u2014 a DEFENSIVE addition that reduces the chance a credential reaches the screen. `ApiKey::new(\"test-key\")` appears only in tests. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 507 added / 109 removed, and its POINT is a defect fix this audit can verify: `call_graph_tool` is DELETED and replaced by `crate::mcp_tool::call`, which returns a three-state `Outcome` \u2014 so the server's `CANNOT ANSWER: ` refusal can no longer be split on newlines and drawn as three rows of chokepoint filenames. That is a narrowing of what this module will assert, not a widening. The rest is `cols`-built service and related-issue tables with named width constants. The 2 network hits are `Client::new` and a `fetch(` call INSIDE `#[cfg(test)]` (the test module starts well above them), the 3 URL hits are `url` fields in a test JSON fixture (`https://api.fatelabs.ca/health`, `https://db/health`, `https://worker/health` \u2014 the last two are not even resolvable names), and the 1 credential hit is `ApiKey::new(\"test-key\")` in the same fixture. Mechanically 0 NEW production destinations, 0 process/exec, 0 filesystem writes, 0 environment reads. RE-ATTESTED 2026-09-03 by coach for a TEST-ONLY change inside the existing `#[cfg(test)]` module. The delta adds two helpers -- `mount_graph_tool_counted(expect_calls: Option<u64>)` carrying the previous body, and `mount_graph_tool_uncounted` -- and switches ONE test, `refuses_a_vacuous_graph_receipt`, to mount its three 'measured row' tools without a call-count expectation. MEASURED CAUSE: `fetch` joins its four graph-tool calls with `tokio::try_join!`, which short-circuits on the first Err and DROPS the siblings, so the vacuous-core_files case cancels blast_radius/chokepoints before their requests leave the client and `.expect(1)` was asserting a SCHEDULING race -- runs 33785692730 and 33785737381 failed on it while 33785604771 PASSED at the same SHA 1915ec8e4. `core_files` KEEPS `.expect(1)` (it is the call that produces the error, so it is always issued) and `reads_all_four_graph_tools_through_the_real_client_type` keeps all four counted mounts, so a change that merely deleted assertions would still fail there. Zero production lines touched: 0 new network destinations, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential sources. The refusal assertion itself is unchanged."
    },
    {
      "path": "tui/src/provider_catalog.rs",
      "blob": "99ca47a9bfe5b65f686c31e42dd5d8349b48175f",
      "reason": "one static provider catalog owns canonical ids, globally unique aliases, acquisition kinds, UI surfaces, server identities, base defaults, and fail-closed HTTP endpoint policy RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them."
    },
    {
      "path": "tui/src/provider_keys.rs",
      "blob": "34676155e2495741df96b31b55cb0b64f22ba121",
      "reason": "provider secrets cross only the authenticated POST /key boundary and receipts expose status/model but never the submitted value"
    },
    {
      "path": "tui/src/provider_store.rs",
      "blob": "d94cb925eda3527d4a8c4bec3fcb9395c81d186d",
      "reason": "Claude, Copilot, and local-provider snapshots share one collision-resistant atomic JSON transaction with a mode-0700 parent, mode-0600 file, zeroized encoded buffer, sync-before-rename, and failed-temporary cleanup RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them."
    },
    {
      "path": "tui/src/public_widgets/composer_input.rs",
      "blob": "2ea063af08dd30e2d3062bcf02568eeeec282e23",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 65 added / 3 removed: two entries added to the composer's completion catalog \u2014 `doctor` and the NAMESPACE entry `skill:` \u2014 plus the tests around them. The `skill:` entry is what `slash_commands::find_external_namespace` keys on, so the composer offers the namespace and stops refusing its members. The 2 process hits are FALSE POSITIVES read individually: both are `ComposerCommand::new(...)`, whose name merely CONTAINS the substring `Command::new`; there is no `std::process` import in this file. Mechanically 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads."
    },
    {
      "path": "tui/src/public_widgets/history_transcript.rs",
      "blob": "f657a14a86c870e060c3179fb8635e8f1f14a099",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-08-29: one line-scoped allow for a fixed dark swatch rendered over a hardcoded ground, with the reason inline. No behaviour change. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 253 added / 6 removed: the tool-call marker vocabulary as named constants (`\u23fa `, `  \u23bf  `, an indent), and `band_the_message_only` \u2014 the fix for a band the founder photographed as THREE lit strips where the spec says one. Reading `transcript.rs` would not have found it: that module builds one background and no padding, and the extra rows come from `history_cell` framing every user turn with a blank at each end while this file re-banded those blanks too. Two files each doing something reasonable and a band nobody wrote. The fix clears the inherited fill on the padding rows, including on their SPANS, and three added tests assert on buffer cells rather than on the code. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/public_widgets/mod.rs",
      "blob": "aa60e188146709076c9f3d1308522092fb54cc0c",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/resume_picker.rs",
      "blob": "df3c96c2eb3a0dac55cfd5b803f7a2ebf05054f4",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 21 added / 16 removed. Three shapes, all local: the gutter literal `\"  \u2502 \"` becomes the const `TRANSCRIPT_GUTTER` at its six call sites, so a glyph change cannot land in five places out of six; a branch selecting a last-line prefix is deleted because ITS TWO ARMS WERE IDENTICAL \u2014 inherited at `50edb00a7` and dead from the day it landed, and a branch whose arms agree is a claim that they differ; and one copy string, `No results for your search` to `No results`. No session is read, listed, resumed or forgotten differently. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/screens.rs",
      "blob": "d82dfe3f3f363554d3fc4e893aab51ecd9bf81be",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-08-29: two bare tuple row types replaced by named aliases (RoutedModelRow, LocalModelRow) to satisfy clippy::type_complexity. Type names only; no behaviour change. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 1,283 added / 144 removed: the catalog screens rebuilt on `crate::cols` with a public `render(idx, palette, tick, pulse)` entry point, and screens 9 and 10 delegating to the PRODUCTION renderers (`gate_refusal::lines`, `orchestra_view`, `work_plan`) rather than drawing fixture strings beside them. Three read-and-cleared hits: the 1 network hit is the string `\"reqwest::Client::retry()\"`, a hallucinated API used as the gate-refusal fixture's claim (no `reqwest` import exists in this file); the 1 URL hit is the text `scripts/deploy.sh` in an evidence cell; the 1 credential hit is the cell value `\"auth\"`. Mechanically 0 real network calls, 0 URL or host literals, 0 process/exec, 0 filesystem writes, 0 environment reads."
    },
    {
      "path": "tui/src/session_gap.rs",
      "blob": "db48f175ff1f31aa5ba42ca1bfbfe7481605c69a",
      "reason": "the Rust brief contract carries a Python-produced release oracle while retaining live cross-repo parity when the server source is present RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 3 added / 6 removed \u2014 a net DELETION. Three human-readable strings lose their greeting and their second person: `Welcome back. You were away {}.` becomes `Away {}.`, `No committed changes were found while you were away.` becomes `No commits landed in that window.`, and `Elsewhere while you were away:` becomes `Elsewhere in that window:`. Every `format!` keeps its placeholders. No gap computation, no commit range and no read of the repository changed. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/session_server.rs",
      "blob": "902325a351c327c87c48a223484755d143a7fa30",
      "reason": "the prior ImportHistory confinement remains unchanged; detached-session fixtures now require the new server-owned /turn/route request before /deep-search and prove the answer still persists across disconnects and named sessions. This is test-only drift and adds no runtime destination, credential read or background execution. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 7 added / 0 removed: one field initialiser `code_currency: None` plus six lines of comment. The field is the new optional staleness verdict on the reply type (see the `estelle-client/src/types.rs` row); the persisted `WireAnswer` this server wrote to disk predates it and carries no verdict, so a REPLAYED answer discloses nothing rather than claiming a repository was current. \u26a0\ufe0f The comment states the residual gap out loud rather than hiding it: an answer that was decertified when first given comes back CERTIFIED on replay, and closing that needs the persisted shape to carry the block. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/setup_flow.rs",
      "blob": "d9f9d6a7517bc085d142dcb5877dd1a4c375b07f",
      "reason": "NEW FILE, pure parser only: derives one proving question from a strong symbol actually present in collected TypeScript or Go source and refuses generic or unparseable input. It performs no file write, network call or credential access. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: delta against the attested blob is 78 added / 1 removed, from 2b33091d7, which stopped setup refusing every repository that was neither TypeScript nor Go. Mechanical scan is 0 on every axis: 0 URL or host literals, 0 endpoint paths, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment or credential reads \u2014 the change is local language detection, and every route it feeds was already declared. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 29 added / 4 removed and it is ENTIRELY RUSTFMT: three `for prefix in [...]` array literals re-flowed from packed lines to one element per line. Every element is byte-identical and in the same order \u2014 the modifier list, the declaration-keyword list and the type-keyword list all parse to exactly the same set. No parser rule, no language detection and no refusal changed. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/slash_command.rs",
      "blob": "900090cff273af31f0bc89d8c67b6b5f141d9903",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration: delta against the attested blob is 5 added / 5 removed, from 82858e2db, rewriting five command descriptions that carried another company's product name. Mechanical scan is 0 on every axis: 0 URL or host literals, 0 endpoint paths, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment or credential reads."
    },
    {
      "path": "tui/src/status/tests.rs",
      "blob": "fb0f2f4754ee53c87593688f0aa994dc5dc78e2a",
      "reason": "release-version snapshot sanitization preserves rendered width when SemVer grows, keeping inherited status layout assertions stable without accepting changed snapshots"
    },
    {
      "path": "tui/src/theme.rs",
      "blob": "a9228d2b16ac5373682cb3671191cca78f510ec1",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-08-29: a module-level `#![allow(clippy::disallowed_methods)]` plus the doc comment arguing for it. The workspace forbids Color::Rgb to keep ANSI portability; this module IS the brand palette (exact identity colours) and adapts via ScreenTheme Dark/Cream chosen from the DETECTED terminal background. No behaviour change, no destination, no credential. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 510 added / 12 removed: `PulseShape` as a value with two named shapes (`ATTENTION`, and `DREAD` \u2014 the slow heavy cycle the founder asked for on a red error), `pulse_with`, a `dampen` that now takes an explicit keep-percentage, and a `contrast` helper. The additions are overwhelmingly TESTS asserting properties of the palette rather than its values: the cream accents are never weaker than their dark twins, no two accent roles render the same colour in either theme, the cream gutter is stronger than the band it labels, and \u2014 the control that keeps the refactor honest \u2014 `the_attention_pulse_kept_the_timing_it_had_before_the_shape_became_a_value`. The 2 credential hits are the word `token` in the DESIGN-TOKEN sense. Mechanical scan otherwise ZERO on every axis: colours and timing arithmetic only."
    },
    {
      "path": "tui/src/top_level.rs",
      "blob": "864a490ea75427ae2a3c15a88ede12f1c14ac138",
      "reason": "`brief` is a repository-confined local file transform; `setup` deliberately composes the already-audited authenticated /mcp, /ingest/start, /ingest/progress and /deep-search routes. The 4,000-file bound now reserves TypeScript and Go representation before filling in Git order, and doctor labels its accepted/total inventory as local-only rather than server proof. No new network destination or credential source was added. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. RE-ATTESTED 2026-08-30: classifies `Leaked` with the other commands that never enter authenticated top-level dispatch; both match arms make accidental transport wiring a compile-time exhaustiveness failure. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration. Delta against the attested blob is 130 added / 3 removed, carrying AFFINITY's `hardware advice` command and multimodal content-block handling. Characterised mechanically: 0 new URL or host literals and NO new endpoint path literal of any kind, 0 new process/exec, 0 new filesystem writes. \u26a0\ufe0f A first lexical pass reported 3 new network calls and 1 credential read; ALL FOUR WERE FALSE POSITIVES and were cleared by reading the lines: `token` appears in a doc comment warning that the field routinely contains env dumps and tokens, and the three `.get(` matches are `block.get(\"source\")`, `.get(\"media_type\")` and `.get(\"data\")` \u2014 JSON map lookups, not HTTP GETs. A grep cannot tell a map read from a request; the count is recorded here so the next reader knows the check was run AND resolved, not skipped. RE-ATTESTED 2026-08-31 by coach during the R11 CLI integration, for the ground-hook transport-failure fix. Delta against the attested blob is 553 added / 71 removed and is the UNION of several lanes' work plus this change. Characterised mechanically: 0 new endpoint path literals, 0 new process/exec, 0 new filesystem writes and 0 new production destinations. The 7 URL/host literals are ALL non-destinations: three are doc comments quoting reqwest's own `error sending request for url (...)` Display \u2014 the very text this change stops interpolating \u2014 and four are TEST fixtures aimed at loopback `127.0.0.1:1`, RFC 2606 `estelle-no-such-host.invalid` and RFC 1918 `10.255.255.1`, addresses chosen precisely because they must FAIL, so refused / dns / timeout are told apart by proof rather than by assertion. \u26a0\ufe0f DISCLOSED RATHER THAN COUNTED: one added human-readable message mentions the path `/admin/load` as advice to a reader; no request is made to it and it is never used to build a URL. The lexical scan MISSED it because it sits mid-sentence inside a format! string, and a scan that only sees quote-leading paths would have let it through silently \u2014 which is why it is written here by hand. The 8 network-call and 8 credential-read hits were read line by line and all are false positives: `reqwest::Error` and `reqwest::StatusCode` are TYPE mentions inside a pure classifier and its fixtures, `reqwest::Client::new()` occurs only under `#[cfg(test)]`, `api.post_scoped(Endpoint::Search, ...)` is another lane's search command on an already-declared route, and the credential hits are the previously-attested `--key` inline flag plus one test fixture query string carrying `account=secret-account-id`, which exists to PROVE no user-visible line carries it. \u26a0\ufe0f Mechanical characterisation only \u2014 the other lanes' semantics remain theirs to review. RE-ATTESTED 2026-09-03 by FORK-AUDIT, and READ AGAINST THE CAPABILITY QUESTION because this is where the hooks live. Delta against the attested blob is 1,726 added / 216 removed. THE CAPABILITY CHANGE: `ground_hook` gains a `GroundOutput`/`ground_envelope` path that can emit `hookSpecificOutput.permissionDecision: \"deny\"` with a mandatory non-empty `permissionDecisionReason` (a `DENY_REASON_FALLBACK` exists precisely so an empty reason can never ship an invalid deny envelope), wired to `ground_block::flagged_outcome` \u2014 so with `ESTELLE_HOOK_BLOCK` opted in AND the index current for the repo, the hook now REFUSES an edit. Default OFF; see the `tui/src/ground_block.rs` row for the full disclosure. NEW ENVIRONMENT READS, 2 at runtime: `ESTELLE_GATE_DISABLED` (presence-tested, an off-switch for the context hook) and `ESTELLE_REGENERATE_PLUGIN_HOOKS` (inside a `#[test]`, guarding the one `std::fs::write` in the delta, which regenerates `estelle-plugin/hooks/hooks.json` from the single hook table and then PANICS so an ordinary run can never move the file). The other 2 environment hits are compile-time `env!(\"CARGO_MANIFEST_DIR\")`. The other 3 filesystem writes are tempdir fixtures under `#[cfg(test)]`. NETWORK: 9 hits, all read \u2014 the only production one is `post_scoped::<Value, Value>(Endpoint::Search, ...)` inside `context_hook`, which is the ALREADY-DECLARED `hook-context` sink (`https://api.fatelabs.ca/search`, source_file `tui/src/top_level.rs`); every `Client::new` and `reqwest::Client::new()` is past the `#[cfg(test)]` boundary. `estelle-client/src/endpoint.rs` still MATCHES its attested blob, so no route was added. TWO CHANGES THAT MOVE A BOUND, DISCLOSED IN BOTH DIRECTIONS: `CONTEXT_HOOK_BUDGET` goes from a literal 4s to `CONTEXT_HOOK_HOST_TIMEOUT_SECS (30) - CONTEXT_HOOK_MARGIN_SECS (10)` = 20s \u2014 a LARGER budget on the hot path of every prompt \u2014 landing together with `context_search_body` adding `\"code\": false`, which the module documents as measured at 133.4s -> 8.4s against production because the server reads `body.get(\"code\", True)` and an ABSENT key means TRUE. And `sweep_estimate_payload` is new but NARROWS what leaves the machine: it sends `{path, bytes}` per file, not content, bounded by the pre-existing `INGEST_MAX_FILES` and asserted. The 6 URL hits are one doc-comment citation plus five NEGATIVE-TEST fixtures in the shell-guard contract (`https://evil.sh`, `https://x.io/i.sh`, `http://x`, `https://api.x.io/health`, and the `railway up` advice string) that exist to prove the guard FIRES on them. \u26a0\ufe0f This is a mechanical characterisation of a delta that is the union of several lanes' work; the semantics of each lane's feature remain theirs to review. [re-reviewed 2026-09-04 for the 0.3.0 release: the delta since the previous release point adds NO new egress sink and NO process spawn. Verified by diffing every changed file for reqwest/hyper/Command::new/process::Command/fs::write/File::create on the Rust side and https./fetch(/spawnSync/execSync on the JS side. The single new network call is the npm-shim lazy binary download in bin/estelle.js, and it targets github.com/uqeu/estelle-cli/releases, a host already declared in docs/egress-sinks.toml.] RE-ATTESTED 2026-09-05 by coach for the v0.3.1 Codex release. Delta against the attested blob is 1,182 added / 110 removed and is the UNION of three fixes plus main's merged work: transcript_path typed Option<String> with a null_is_absent deserializer on every host-supplied field (Codex sends an explicit null and serde's #[serde(default)] covers an ABSENT key, not a present one, so one field killed all eight hook modes); ContextOutcome replacing Vec<String> so a failed context hook renders a non-empty envelope instead of one byte with exit 0; and PORTABLE_HOOK_RUNNER replacing std::env::current_exe() so install-hooks stops writing a path that exists on one machine. CHARACTERISED MECHANICALLY over the added lines: 0 new URL or host literals, 0 new endpoint path literals, 0 new process/exec. \u26a0\ufe0f TWO hits were flagged and BOTH were read rather than waved through: the single filesystem write is fs::write(&transcript, raw) building a fixture transcript inside a test, and the single credential-shaped hit is the literal ApiKey::new(\"test-key\") in a test constructor. Neither is a production path. The count is recorded here so the next reader knows the scan was run AND resolved, not skipped."
    },
    {
      "path": "tui/src/transcript.rs",
      "blob": "4b1f8648b5102070674c572af61a98425514ef3b",
      "reason": "Applies `mask_secret` to every rendered line, and detects http/https prefixes only to STYLE them as links. Defensive; adds no fetch of any kind. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 195 added / 30 removed, and the substantive addition is `stale_lines` \u2014 the renderer for the server's `code_currency` staleness verdict, which had been on the wire with no CLI surface reading it. It draws the verdict ABOVE the answer it decertifies, so a reader cannot take the citation before the caveat. Pure `Vec<Line>` construction over an already-parsed type. Mechanical scan is ZERO on every axis: 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads."
    },
    {
      "path": "tui/src/transcript_adoption_tests.rs",
      "blob": "ba06b234f3998078b17fa773d13545180f120302",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Test-only code. URL literals are fixtures on reserved example domains (example.com, example.test, payments.example) or 127.0.0.1; API keys are the literal string \"test-key\". RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 109 added / 7 removed, TEST-ONLY. One test is renamed and rewritten to ask the palette for its `cite` token instead of pinning a literal blue \u2014 the one-owner move again \u2014 and two are added: a markdown table in an answer renders as a table, and a fenced diagram PRINTS ITS SOURCE because nothing in a terminal draws one, which is the honest behaviour rather than a wrong picture. Mechanical scan is ZERO on every axis."
    },
    {
      "path": "tui/src/tui.rs",
      "blob": "5450eae0416c7b0a27d67c3d43a0a0cf0d108b2a",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/version_check.rs",
      "blob": "8e3d77c962224f923ca5130e3f6558deae7d1d75",
      "reason": "NEW FILE, and it widens egress by exactly one host. Unauthenticated GET to api.github.com/repos/uqeu/estelle-cli/releases/latest, bounded by a 3s timeout and a 256KB response cap, cached 24h, suppressible with ESTELLE_NO_VERSION_CHECK, sending no account, credential, repo, prompt or file content. It NEVER downloads, writes or executes a binary \u2014 it prints a command and stops, asserted by a test that fails if the message ever grows an auto-apply prompt. Recorded as the `cli-version-check` sink. RE-ATTESTED 2026-08-29 after a blob drift. Mechanical egress scan of the drifted set found its only live destinations to be api.fatelabs.ca, api.github.com, docs.github.com, registry.npmjs.org and github.com \u2014 all already-declared sinks. Remaining URL literals are negative-test fixtures (attacker.invalid, evil.sh, provider.example, example.com, localhost/127.0.0.1) that exist to prove validation REJECTS them. [re-reviewed 2026-09-04 for the 0.3.0 release: the delta since the previous release point adds NO new egress sink and NO process spawn. Verified by diffing every changed file for reqwest/hyper/Command::new/process::Command/fs::write/File::create on the Rust side and https./fetch(/spawnSync/execSync on the JS side. The single new network call is the npm-shim lazy binary download in bin/estelle.js, and it targets github.com/uqeu/estelle-cli/releases, a host already declared in docs/egress-sinks.toml.]"
    },
    {
      "path": "tui/src/work_job.rs",
      "blob": "ea30f6e30cf893422dda4375728a959694ea625c",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added."
    },
    {
      "path": "tui/src/work_plan.rs",
      "blob": "f0d0af52d4c99d21eb7e3a0665d7680f80e4950e",
      "reason": "Mechanical egress scan on 2026-08-29 found no new network destination and no new credential source in this file. Terminal UI composition \u2014 layout, theme, screens, slash-command routing and history rendering. No I/O added. RE-ATTESTED 2026-09-03 by FORK-AUDIT. Delta against the attested blob is 255 added / 53 removed: the plan table moves from one hardcoded `PLAN_COLUMNS` to a width-derived `plan_columns(width)` built from six named constants, gains `lines_at` so the caller can pass the real session-column width, and replaces a `status()` string-pair helper with `StepMark`, the shared vocabulary. Four added tests, including one that asserts the evidence gutter survives EVERY width the session column offers and one asserting the warnings are derived from the plan rather than invented. Mechanical scan is ZERO on every axis: 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/mcp_server_elicitation.rs",
      "blob": "07a83f7d166ab0450e74c4041d54cd8bbd376d6b",
      "reason": "COPY PASS, 2 added / 2 removed, both `const &str` values: ANSWER_PLACEHOLDER `Type your answer` -> `Type an answer` and OPTIONAL_ANSWER_PLACEHOLDER `Type your answer (optional)` -> `Type an answer (optional)`. Second-person removal only; the constant names, their types, every reader of them and the elicitation control flow are byte-identical. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/memories_settings_view.rs",
      "blob": "1c99358631275cb8d0c12480e96fb8ed57f23229",
      "reason": "COPY PASS, 2 added / 2 removed, both human-readable strings inside `.dim()` help lines: `Choose how Codex uses and creates memories. Changes are saved to config.toml` -> `Choose how Estelle uses and creates memories. Saved to config.toml`, and `... for the current Codex home.` -> `... for the current Estelle home.` This is the brand pass taking another company's product name out of our binary. No identifier, no path and no clearing behaviour changed - the sentence describing what the clear does is the same claim in fewer words. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/mod.rs",
      "blob": "d1cc52ccee9537a4c24d9d07083227be325da6e7",
      "reason": "COPY PASS, 1 added / 1 removed, one `.to_string()` literal: `Install this app in your browser, then return here.` -> `Install this app in the browser, then return here.` Second-person removal only. Nothing in this module's dispatch, layout or view-stack handling is touched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/request_user_input/mod.rs",
      "blob": "19dad3467be2f096ebf25f1ce0a11a783f6304ba",
      "reason": "COPY PASS, 2 added / 2 removed: the `ANSWER_PLACEHOLDER` const `Type your answer (optional)` -> `Type an answer (optional)`, and the one rendered example line inside this file that quotes it, kept in step so the fixture still matches what the widget draws. Second-person removal only; no field, no key handling and no submission path changed. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/skills_toggle_view.rs",
      "blob": "5d318938c3426badb1c8dc4f8efc955d26c59ef9",
      "reason": "COPY PASS, 1 added / 1 removed, one `.dim()` help string: `Turn skills on or off. Your changes are saved automatically.` -> `Turn skills on or off. Changes save automatically.` Second-person removal only; the toggle, its persistence and every enabled/disabled decision are untouched, and the sentence still makes the same claim about saving. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/status_line_setup.rs",
      "blob": "ed58e6c83f7867facb8569ec151e202d907ca385",
      "reason": "COPY PASS, 1 added / 1 removed, one match-arm LABEL: `StatusLineItem::CodexVersion => \"Codex application version\"` -> `... => \"Estelle application version\"`. The ENUM VARIANT NAME `CodexVersion` is deliberately unchanged, so no other file, no serialised config key and no match exhaustiveness is affected - only the string a human reads. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/title_setup.rs",
      "blob": "d1f7aa6d04d51120353eb9285c198a91ef3b524d",
      "reason": "COPY PASS, 2 added / 2 removed, two match-arm LABELS: `TerminalTitleItem::AppName => \"Codex app name\"` and `TerminalTitleItem::CodexVersion => \"Codex application version\"` both take `Estelle`. As in status_line_setup.rs the variant names are unchanged, so the config surface and the exhaustiveness check are identical; only the human-readable label moved. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/connectors.rs",
      "blob": "45dbf0c42f6fed73436354367dedb2717725d6a2",
      "reason": "COPY PASS, 3 added / 3 removed, three human-readable strings: `Use $ to insert an installed app into your prompt.` -> `Type $ ... into the prompt.`, `Manage this app in your browser.` -> `... in the browser.`, `Install this app in your browser, then reload Codex.` -> `... in the browser, then reload Codex.` Second-person removal only. No connector is added, removed or reordered, and no install/manage action is wired differently - these lines only describe actions the user performs elsewhere. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/ide_context.rs",
      "blob": "f629d28c179ef326f6c5fb76804c322e482f6f24",
      "reason": "COPY PASS, 2 added / 2 removed, two human-readable strings describing IDE attachment: `Future messages will include your current IDE selection and open tabs.` -> `Future messages include the open IDE tabs and the current selection.`, and `Connected to your IDE.` -> `Connected to the IDE.` The first sentence is a DISCLOSURE of what gets sent, and the rewrite states exactly the same two items (open tabs, current selection) - it neither widens nor narrows the claim, and no code deciding what is attached was touched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/model_popups.rs",
      "blob": "33d64891bc56ef8473fad328c6d8284a8bde492e",
      "reason": "COPY PASS, 2 added / 2 removed, two human-readable strings: `Models are being updated; please try /model again in a moment.` -> `The model list is updating. Run /model again.`, and `Access legacy models by running codex -m <model_name> or in your config.toml` -> `Legacy models: run codex -m <model_name>, or name one in config.toml`. The `-m` flag and `config.toml` remain the same two instructions; no model id, no catalog fetch and no popup state machine changed. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/notifications.rs",
      "blob": "efc720f9544a0fc705c41290b7c5d684bd21e1f5",
      "reason": "COPY PASS, 1 added / 1 removed, one `format!` template: `Codex wants to edit {}` -> `Estelle wants to edit {}`. Brand pass; the positional argument, the notification trigger and every approval decision downstream are untouched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/permission_popups.rs",
      "blob": "f65ba727ddbc8746763390b7c44900a158db2e07",
      "reason": "COPY PASS over SECURITY-FACING TEXT - read twice for that reason. 2 added / 2 removed, both human-readable warnings in the sandbox popup. `The non-admin sandbox protects your files and prevents network access under most circumstances. However, it carries greater risk if prompt injected. To upgrade to the default sandbox, run ` -> `The non-admin sandbox protects files and blocks network access under most circumstances. It carries greater risk under prompt injection. To upgrade to the default sandbox, run `; and `When Codex runs with full access, it can edit any file on your computer and run commands with network, without your approval. ` -> `With full access, Codex edits any file on this machine and runs networked commands without approval. ` Both rewrites preserve every clause that carries risk information: the `most circumstances` hedge on network blocking, the prompt-injection caveat, and the unqualified statement that full access edits ANY file and runs NETWORKED commands with NO approval. Nothing was softened or dropped. No sandbox policy, no approval threshold and no popup branch is touched - this file only renders the warning. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/reset_credits.rs",
      "blob": "8eb0f977c1a048dbd09f5ba0ba120cd235c4298d",
      "reason": "COPY PASS, 2 added / 2 removed, the same sentence in its two occurrences (the `unwrap_or` fallback description and the constructed `description` field): `Reset your current usage limits.` -> `Reset the current usage limits.` Second-person removal only; the reset command itself, its route and its confirmation are untouched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/safety_buffering.rs",
      "blob": "69e0e47035563ba62dd045c0b8eb6b76171ba45e",
      "reason": "COPY PASS, 4 added / 4 removed (the extra line each way is rustfmt re-wrapping a shortened literal), three `const &str` values: SAFETY_BUFFERING_HEADER `Our systems are thinking a bit more about this request before responding.` -> `An extra safety pass is running on this turn.`; SAFETY_BUFFERING_MESSAGE_WITH_RETRY -> `Retry on a faster model for a quicker answer, at lower capability.`; SAFETY_BUFFERING_FOOTER -> `Still waiting. This closes when the response lands.` The rewrite names the mechanism (a safety pass) instead of anthropomorphising it, and keeps the same two facts: a faster model trades capability for latency, and the menu closes by itself. No buffering threshold, retry path or model choice is implemented here. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/session_flow.rs",
      "blob": "2b8a1289d750efa7b44e5cb08d919032fa100d4f",
      "reason": "COPY PASS, 1 added / 1 removed, one `.into()` literal: `You're continuing from this point in a new conversation` -> `A new conversation continues from this point`. Second-person removal only; the fork/continue behaviour it labels is unchanged. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/settings_popups.rs",
      "blob": "258289498bbbd934066e62aa2e04524c0e2b183e",
      "reason": "COPY PASS, 3 added / 1 removed: one header string `Choose a communication style for Codex.` -> `Choose a communication style for Estelle.`, with the two extra added lines being rustfmt breaking the now-longer `Line::from(...)` call across three lines. Brand pass; no setting, no persisted key and no popup branch changed. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/keymap_setup.rs",
      "blob": "f0877b498df6f6a94048d5c198ee8b2e6bd66d21",
      "reason": "COPY PASS in TEST CODE, 2 added / 2 removed, two assertions re-pinned to the hint text rewritten in keymap_setup/debug.rs: `assert!(rendered.contains(\"Still waiting?\"))` -> `contains(\"If nothing changes on a keypress\")`, and the negative assertion `assert!(!rendered.contains(\"Still waiting?\"))` -> `!contains(\"Only received keys can be assigned as shortcuts\")`. The pair still asserts the same thing - the delayed hint appears only after the delay - and the negative control is retained rather than deleted, which is the property that matters here. No production code in this file changed. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/keymap_setup/debug.rs",
      "blob": "93af9019b028db55fdecdd1b8c7ef45be0ceeb09",
      "reason": "COPY PASS, 2 added / 2 removed, two `const &str` hints: SHORT_MISSING_KEY_HINT `Tip: Codex can only inspect keys your terminal sends.` -> `Codex can only inspect keys the terminal sends.`, and DELAYED_MISSING_KEY_HINT `Still waiting? If nothing changes when you press a key, your terminal is not sending that key to Codex. Only received keys can be assigned as shortcuts.` -> `If nothing changes on a keypress, the terminal is not sending that key to Codex. Only received keys can be assigned as shortcuts.` Second-person removal; the diagnostic claim is unchanged, and the assertions in keymap_setup.rs were updated in the same pass so the test still covers both hints. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/keymap_setup/picker.rs",
      "blob": "4564d6df4134ad925949cd107c888acc7f16bad3",
      "reason": "COPY PASS, 1 added / 1 removed, one `.to_string()` literal: `Inspect keypresses from your terminal.` -> `Inspect keypresses from the terminal.` Second-person removal only; the picker's key capture is untouched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/diff_render.rs",
      "blob": "48f4ec29e9b0c42426cd0e5df7a12731d0bb4cc9",
      "reason": "GLYPH PASS, 1 added / 1 removed: the tree prefix `\"  \u2514 \"` -> `\"  \u2502 \"` in the one header push in this file. U+2514 and U+2502 are both single-column box-drawing glyphs of 3 UTF-8 bytes, so the string stays 4 characters and 4 display columns wide and no width arithmetic anywhere is affected - the change is which line the eye follows, not how much room it takes. No diff parsing, hunk selection or file handling is touched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/exec_cell/render.rs",
      "blob": "c9c05f832e5c938d449e232f8516d3eddf2b7f7c",
      "reason": "GLYPH PASS, 3 added / 3 removed: the same `\"  \u2514 \"` -> `\"  \u2502 \"` substitution at its three occurrences here - the bare prefix, the `prefix_lines(out_indented, ...)` call and the `PrefixedBlock::new(\"  \u2502 \", \"    \")` construction. Same-width substitution (see diff_render.rs), and the continuation indent `\"    \"` is unchanged, so the block still aligns. No command text, exit status or output capture is touched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/history_cell/exec.rs",
      "blob": "1514000db2b497cb9686ba8609aadbad8493d670",
      "reason": "GLYPH PASS, 1 added / 1 removed: `initial_indent(Line::from(\"  \u2514 \".dim()))` -> `\"  \u2502 \".dim()`. Same-width substitution; the wrapper's subsequent-indent is unchanged. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/history_cell/mcp.rs",
      "blob": "3bd9462e65bf06d1eb1d0444719ed4190970b984",
      "reason": "GLYPH PASS, 3 added / 3 removed: two `\"  \u2514 \"` -> `\"  \u2502 \"` substitutions plus the COMMENT above them updated in step, so the note `Reserve four columns for the tree prefix (\"  \u2502 \"/\"    \")` still quotes the string it describes. That comment is the reason to check the width claim, and it holds: both glyphs are one display column, so four columns are still exactly what is reserved and the `at least one cell` guard behaves identically. No MCP tool invocation, argument rendering or result handling is touched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/history_cell/patches.rs",
      "blob": "88ee9d456229dc2b865f395c6e397b36dc64de18",
      "reason": "GLYPH PASS, 3 added / 3 removed: `\"  \u2514 \"` -> `\"  \u2502 \"` at its three occurrences (the display-path line, the detail line and the `Saved to: ` line). Same-width substitution; the paths and the `Saved to: ` label they prefix are unchanged, and no patch is applied, written or parsed differently. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/history_cell/plans.rs",
      "blob": "0cca20e8919df67cfd2def489a798cee5cebad20",
      "reason": "GLYPH PASS, 1 added / 1 removed: `prefix_lines(indented_lines, \"  \u2514 \".dim(), \"    \".into())` -> `\"  \u2502 \".dim()`. Same-width substitution; plan content and step state are untouched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/multi_agents.rs",
      "blob": "7afa4d4b15c9296434016807a100d24ba56149e2",
      "reason": "GLYPH PASS, 1 added / 1 removed: `prefix_lines(details, \"  \u2514 \".dim(), \"    \".into())` -> `\"  \u2502 \".dim()`. Same-width substitution. No agent is spawned, addressed or scheduled differently by this file's change; it renders detail lines only. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/status_indicator_widget.rs",
      "blob": "a7e27f6c5fb9ec583471b33f9952eec7499acc39",
      "reason": "GLYPH PASS, 1 added / 1 removed: `const DETAILS_PREFIX: &str = \"  \u2514 \"` -> `\"  \u2502 \"`. This is the const the other seven glyph-pass files match, and the same-width argument applies (U+2514 and U+2502 are both one display column, 3 UTF-8 bytes). No status computation, timer or spinner behaviour changed. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": ".github/workflows/ci.yml",
      "blob": "28b8120ef3018498d44c3a56e4c2c5bf3bd1a6cd",
      "reason": "NEW WORKFLOW, and the only NEW CAPABILITY in this batch \u2014 read closely for that reason. 66 added / 0 removed. Until this file existed, every workflow in this repository was `workflow_dispatch:` only, so `cargo fmt`, `cargo clippy` and `cargo test` fired once per release, by hand. This adds `on: push` (branches-ignore `gh-readonly-queue/**`), `pull_request` and `workflow_dispatch`, i.e. it makes CI run automatically. TRUST SURFACE, itemised: `permissions: contents: read` at the top level and no job-level widening, so the job's GITHUB_TOKEN cannot write to the repository, cut a tag or publish a release; NO `secrets.*` reference anywhere in the file, so a `pull_request` run from a fork gets a read-only token and no secret material; `concurrency` with `cancel-in-progress`, and `timeout-minutes: 60`, so a hung job cannot burn indefinitely. THIRD-PARTY CODE: two actions, both pinned to a full 40-character commit SHA rather than a moving tag, and BOTH VERIFIED AGAINST UPSTREAM rather than taken from the comment beside them - `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683` is exactly the commit `refs/tags/v4.2.2` points at, and `Swatinem/rust-cache@f0deed1e0edfc6a9be95417288c0e1099b1eeec3` is the commit the ANNOTATED tag `v2.7.7` dereferences to (`gh api repos/<owner>/<repo>/git/ref/tags/<tag>` then `git/tags/<sha>`). `Swatinem/rust-cache` is new third-party code in our CI path and it does reach GitHub's own Actions cache service; that is disclosed here rather than counted as zero, because a cache action by definition reads and writes an artifact store. It is not a product egress sink and does not appear in docs/egress-sinks.toml for that reason. The four `run:` steps invoke only `cargo`; the last is a bash guard asserting that `-p estelle-tui --lib` DECLARES more than 3000 tests and prints exactly one `test result:` line, because `cargo test` exits 0 when a target aborts before printing its counts. Mechanically 0 URL or host literals of any kind, 0 curl/wget/network invocations, 0 credential or environment reads (RUST_MIN_STACK is deliberately NOT set here - it lives in `.cargo/config.toml` so a developer's bare `cargo test` gets the same floor). \u26a0\ufe0f Scope limit stated out loud: this row attests the workflow's PERMISSIONS, TRIGGERS, PINS and COMMANDS. It does not audit the contents of the two pinned third-party actions. [re-reviewed 2026-09-04 for the 0.3.0 release: the delta since the previous release point adds NO new egress sink and NO process spawn. Verified by diffing every changed file for reqwest/hyper/Command::new/process::Command/fs::write/File::create on the Rust side and https./fetch(/spawnSync/execSync on the JS side. The single new network call is the npm-shim lazy binary download in bin/estelle.js, and it targets github.com/uqeu/estelle-cli/releases, a host already declared in docs/egress-sinks.toml.]"
    },
    {
      "path": "estelle-client/src/repo.rs",
      "blob": "c6424acfcf1a595a70b8c45329183b6b3fcf7204",
      "reason": "41 added / 1 removed, and the one removed line is the inline `\"unknown/repo\"` literal promoted to the new `pub const UNRESOLVED_REPO`. Three additions: that const, `Repo::is_unresolved()` comparing against it, and `pub fn is_repository(root: &Path) -> bool`. NEW PUBLIC FUNCTION, so it was read against the capability question rather than the line count: `is_repository` calls `root.canonicalize()` and `gix::discover(&root)`, both of which are filesystem READS, and `gix::discover` was ALREADY the mechanism inside `repo_for` three lines above - no new library, no new primitive, and specifically still no `git` subprocess, which the pre-existing comment says is deliberate because ACP's cwd is client-supplied and letting it choose which executable resolves a repository is code execution. The wire value `unknown/repo` is explicitly left unchanged; what changed is that the interface now asks `is_repository` before printing a name, because `repo_for` names any directory after itself (run from `~` it answers `khai`) and that lenient answer is pinned by a cross-language parity test against the Python hook. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 FILESYSTEM WRITES (canonicalize and discover read only), 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/app/tests/rate_limits.rs",
      "blob": "7fddf7a90cd81e74edf363d240cb4292ec04d7ad",
      "reason": "TEST-ONLY, 4 added / 4 removed: four `assert!(popup.contains(...))` needles re-pinned to the strings rewritten in chatwidget/usage.rs and chatwidget/rate_limits.rs - `You don't have any usage limit resets available.` -> `No usage limit resets available.`, `Usage reset. You have 0 usage limit resets left.` -> `Usage reset. 0 usage limit resets left.`, and `Request a limit increase from your owner` -> the shorter `limit increase from the workspace owner` in both its positive and its NEGATIVE assertion. The negative control is retained, which is the property that matters: the test still proves a recovered state CLEARS workspace error routing. No test was deleted and no assertion was weakened to a substring of nothing. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/app_link_view.rs",
      "blob": "3cdb17c39d702dcf76cab276f7c436b31eacca86",
      "reason": "COPY PASS, 13 added / 16 removed (net deletion; the imbalance is rustfmt collapsing now-shorter `wrap(...)` calls onto one line). Two shapes: second-person removal in the `instructions` strings (`in your browser` -> `in the browser`, at 2 production sites and 4 test fixtures) and first-person button labels rewritten as states (`I already signed in` -> `Signed in`, `I finished` -> `Finished`, `I already Installed it` -> `Installed`). \u26a0\ufe0f Checked specifically: each button label is quoted back inside the body text that tells the reader which button to press, and all three quotations were updated in the same diff, so the instruction still names a button that exists. No `AppLinkScreen` variant, no URL, no install action and no elicitation id changed - the three `https://...` literals visible in the hunks are UNCHANGED context lines in test fixtures. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/hooks_browser_view.rs",
      "blob": "51f92c3af183d82bfee46ea24844d4c3e3593569",
      "reason": "9 added / 2 removed. Two changes. (1) COPY PASS: `Turn hooks on or off. Your changes are saved automatically.` -> `Changes save automatically.` (2) TEST FIX with a 7-line comment: an assertion pinning the selected row's foreground to the LITERAL `Some(Color::Cyan)` - the accent as it stood at the `50edb00a7` import - now asks the shared owner, `accent_style().fg`. `ebf7d5108` had moved the accent to DARK_ACCENT when the design book took over rendering; the sibling test `selected_event_rows_use_the_shared_accent_style` followed that move for free and this one could not. Replacing a literal with the single owner of the fact is the correct direction, and the assertion still fails if the row stops taking the accent at all. Nothing about hook ENABLEMENT, hook discovery or the review-needed count is touched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/approval_overlay.rs",
      "blob": "8126bab941f2ef88a7a618470b842e3f351a0b5f",
      "reason": "COPY PASS over an APPROVAL SURFACE - read twice for that reason. 18 added / 12 removed. Five prompts lose their chatbot voice (`Would you like to run the following command?` -> `Run the following command?`; `Do you want to approve network access to \"{}\"?` -> `Approve network access to \"{}\"?`; `Would you like to grant these permissions?`; `Would you like to make the following edits?`; `{server_name} needs your approval.` -> `needs approval.`) and four past-tense outcome lines drop `You` (`You granted additional permissions` -> `Additional permissions granted`). Every prompt keeps its SUBJECT: the command, the host, the permission set and the edit list are all still named, and the network prompt still interpolates `{}` with the host. The 2 url_host hits are BOTH in the added test material, not in production: the strengthened assertion now checks `line.contains(\"network access\") && line.contains(\"\\\"example.com\\\"\")` instead of pinning the whole old sentence, and the added comment mentions `evil.example` to explain why the HOST is the thing worth pinning. That is the right direction - a copy edit should not turn this red, a missing host must. No approval THRESHOLD, no default answer and no sandbox policy is touched. Mechanically 0 new production URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/bottom_pane/feedback_view.rs",
      "blob": "fc7274c23467ebfe17a115166728930dfc915664",
      "reason": "COPY PASS, 13 added / 11 removed: `Please report this in #codex-feedback:` -> `Report this in ...`, `Please open an issue using the following URL:` -> `Open an issue at this URL:`, `Share this and add some info about your problem:` -> `Share this along with what went wrong:`, `your thread ID` -> `the thread ID`, and the `None` arm's `Thanks for the feedback!` replaced by the bare prefix, which had already said the only true thing (the feedback landed). Three test expectation strings were updated in step. The 2 url_host hits are UNCHANGED URLs inside those rewritten expectation lines - `https://github.com/openai/codex/issues/new?template=3-cli.yml...`, `http://go/codex-feedback-internal` and `https://go/codex-feedback/thread-2` all appear byte-identical on the corresponding removed lines, so they are inherited upstream fixtures re-quoted, NOT new destinations. No upload path, no request and no artifact contents changed here. Mechanically 0 NEW URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/plugin_catalog.rs",
      "blob": "5b1ffc4d7363afda36d04ddab06f8fbdcf571f77",
      "reason": "COPY PASS, 5 added / 8 removed (net deletion; rustfmt collapses the shortened literals). `No plugins have been shared with you.` -> `No plugins are shared with this account.`, `This removes the configured marketplace from Codex.` -> `... from Estelle.` (brand pass), `Select the plugins you want to use and press Enter...` -> `Select plugins, then press Enter...`, and two admin descriptions lose `This plugin is ... by your workspace admin.` for `Installed by the workspace admin.` / `Disabled by the workspace admin.` No plugin is installed, enabled, disabled or fetched differently; the admin-managed FLAG these lines describe is read elsewhere and untouched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/plugins.rs",
      "blob": "156715b8ab23701b992a1a0e4e9e2047d8efcfc6",
      "reason": "COPY PASS, 4 added / 6 removed (net deletion). `Open the app page in your browser.` -> `in the browser.`, the confirmation item `I've installed it` -> `Installed` with its description `Trust your confirmation and continue to the next app.` -> `Take it as installed and continue to the next app.`, and `You can now continue managing plugins from /plugins.` -> `Manage plugins from /plugins.` \u26a0\ufe0f Read for meaning because the second one describes a TRUST decision: the rewrite is honest about it - `Take it as installed` says the same thing as `Trust your confirmation`, namely that the app's installed state is asserted by the user rather than verified. Neither wording claims verification. No install check was added or removed. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/rate_limits.rs",
      "blob": "84e1be0b0fd7bc97cddcac1b349d845b96692206",
      "reason": "COPY PASS, 7 added / 7 removed, all human-readable limit and credit messages: the two identical `Heads up, you have less than {remaining_percent:.0}% of your {limit_label} limit left.` templates -> `Less than {remaining_percent:.0}% of the {limit_label} limit is left.`; `You've reached your workspace credit limit` -> `Workspace credit limit reached`; `Your workspace is out of credits. Ask your workspace owner to add more.` -> `The workspace is out of credits. The workspace owner can add more.`; `Request a limit increase from your owner to continue using codex.` -> `Continuing needs a limit increase from the workspace owner.`; and two `Please try again.` -> `Run it again.` Both `format!` templates keep both placeholders and the `.0` precision. No threshold, no percentage arithmetic, no notify/request action and no route changed - the notify and increase requests are still offered on exactly the same conditions. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/windows_sandbox_prompts.rs",
      "blob": "93c6028bde9ef3dbe8c937de3c276046fe8e1edc",
      "reason": "COPY PASS over SANDBOX TEXT - read twice for that reason. 7 added / 7 removed. `We couldn't complete the world-writable scan, so protections cannot be verified. ` -> `The world-writable scan did not complete, so protections cannot be verified. ` keeps the CANNOT BE VERIFIED clause verbatim, which is the whole point of that line. `Learn more <url>` -> `Details: <url>` around an UNCHANGED `https://developers.openai.com/codex/windows` - that literal appears byte-identical on the removed line, so it is not a new destination. The remaining four drop `your`/`You` from the admin-permission failure and the organization-requires-default-sandbox lines, and the non-admin warning keeps its risk clause in full (`That carries greater risk under prompt injection.`). No sandbox setup, elevation request or policy check is touched. Mechanically 0 NEW URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/tooltips.rs",
      "blob": "5f4fd204e93eb7ddd430fcc4267b7f3ce905344b",
      "reason": "COPY PASS that NET-REMOVES two promotional URLs, 5 added / 6 removed. `APP_TOOLTIP` and `OTHER_TOOLTIP` both carried `https://chatgpt.com/codex?app-landing-page=true`; both literals are DELETED and replaced by tooltips describing this product's own commands (`/demo`, `/gate`, `/verify`, `/fast`). `FREE_GO_TOOLTIP`'s limited-time offer for another company's plan is replaced with a factual statement about this plan. The added lines contain no URL at all, so the net change to the URL census in this file is -2. The 1 credential hit is the word `token` inside `at no extra token cost` - prose, not a read. A tooltip is inert text; no link is opened from here. Mechanically 0 new URL or host literals (2 REMOVED), 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/tests/app_server.rs",
      "blob": "1ed0003d311a1d7e88089c431c15fe649ecc2b22",
      "reason": "TEST-ONLY, 2 added / 3 removed: the local `SAFETY_BUFFERING_HEADER_TEXT` fixture re-pinned to the new header in chatwidget/safety_buffering.rs, and one assertion needle `extra safety checks are on` -> `extra safety checks run`. Both are the same assertions against the rewritten strings; nothing was deleted or made vacuous. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/tests/exec_flow.rs",
      "blob": "0a06513ff0c56e9885126a12a263b7450f9ca7a2",
      "reason": "TEST-ONLY, 3 added / 3 removed: one GLYPH-PASS expectation `\"\u2022 Explored\\n  \u2514 List ls\\n\"` -> `\"  \u2502 List ls\"`, and two COPY-PASS needles for the approval title `Would you like to make the following edits?` -> `Make the following edits?` (the assertion and its failure message, kept in step so the message still quotes the string it looked for). Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/tests/slash_commands.rs",
      "blob": "ae0b2133602ef542c0b6e243e9d50393ba152597",
      "reason": "TEST-ONLY, 1 added / 1 removed: an inline snapshot for the `/goal` usage line, `The session must start before you can change a goal.` -> `before a goal can change.` Second-person removal in the string the snapshot pins. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/tests/usage.rs",
      "blob": "bf267af0c60d5b2396824071e4dd4a54d9d8930c",
      "reason": "TEST-ONLY, 11 added / 7 removed. Five needles re-pinned to the strings rewritten in chatwidget/usage.rs, plus one substantive fix carried by a 4-line comment: an assertion pinned the literal `\"That reset is gone.\"`, A SENTENCE THE PRODUCT HAS NEVER SAID, so it was testing a string that existed only in this file. It now asserts `crate::chatwidget::usage::STALE_RESET_CREDIT_MESSAGE` - the single owner - so a copy edit cannot turn it red while the message FAILING TO RENDER still does. That is the same one-owner move as hooks_browser_view.rs in this batch, in the opposite direction (a test reaching for the owner rather than a literal). Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/usage.rs",
      "blob": "b0ab96db4a9a11fb9fdbd7ad50be8763fcbece36",
      "reason": "COPY PASS plus one CONST EXTRACTION, 20 added / 13 removed. Ten human-readable strings drop `you`/`your` (`You have {available_count} {} available.` -> `{available_count} {} available.`, `Checking your available resets...`, `Resetting your usage...`, `Your usage does not need a reset right now.`, `Usage reset. You have {available_count} {} left.`, two `Please try again.` -> a specific `Run /usage again.`, etc). Every `format!` keeps its placeholders, including the pluralisation argument. The extraction: the stale-reset sentence becomes `pub(super) const STALE_RESET_CREDIT_MESSAGE`, with a docstring explaining that the test used to pin a DIFFERENT sentence and two owners of one string is one too many. No reset is redeemed, no credit count is computed and no route is called differently; this file assembles subtitles and message params. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/onboarding/trust_directory.rs",
      "blob": "52ff31a3c8ad894310cea0e631eb608c32a5e4c1",
      "reason": "COPY PASS on a TRUST PROMPT - read twice for that reason. 2 added / 2 removed: `You are in ` -> `Working in ` (bold lead-in before the path), and `Note: You're in a subdirectory of a Git project. Trusting will apply to the repository root: {}` -> `Note: this is a subdirectory of a Git project. Trust applies to the repository root: {}`. The second sentence is the one that carries consequence, and the rewrite keeps both facts intact - that the cwd is a SUBDIRECTORY, and that trust attaches to the ROOT, which is still interpolated by the same `{}`. Nothing about how trust is recorded, scoped or checked is touched. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/onboarding/welcome.rs",
      "blob": "3398c4f80a862e753216a5af47f55cdf0c8909f5",
      "reason": "8 added / 6 removed. One production DELETION - the span `\"Welcome to \"` is removed from the first screen, leaving the banner to name the program - plus a `tracing::warn!` message reworded from `Welcome background to press '.'` to `background variant changed on '.'`, and two tests re-pointed from `row_containing(&buf, \"Welcome\")` to `row_containing(&buf, \"Estelle\")`. \u26a0\ufe0f Checked because a test needle changing alongside its subject is how a weakened assertion hides: both assertions still pin an exact ROW INDEX (`Some(frame_lines + 1)` and `Some(0)`), so they prove the banner's POSITION exactly as before and merely look for the word that is now there. No onboarding step, consent or ordering changed. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/onboarding/auth/headless_chatgpt_login.rs",
      "blob": "8d5a49f1831dd57893b9b241dd136fc0ca860cfb",
      "reason": "COPY PASS on a LOGIN SURFACE - read twice for that reason. 4 added / 6 removed (net deletion). `Finish signing in via your browser` -> `in the browser`, `Open this link in your browser` -> `in the browser`, `Enter this one-time code after you are signed in (expires in 15 minutes)` -> `once signed in (expires in 15 minutes)`, and the anti-phishing line `Continue only if you started this login in Codex. If a website or another person gave you this code, cancel.` -> `Continue only if this login started in Codex. If a website or another person supplied this code, cancel.` THE ANTI-PHISHING WARNING KEEPS BOTH HALVES: the positive condition (this login started in Codex) and the explicit cancel instruction when a third party supplied the code. The 15-minute expiry is retained verbatim. No device-code flow, no polling, no token handling and no URL changed - this file renders the instructions beside a flow implemented elsewhere. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/onboarding/auth.rs",
      "blob": "365a71a16632d390b2a4a2f0ca3fb659cb712a36",
      "reason": "COPY PASS on the AUTH CHOICE SCREEN - read twice for that reason. 25 added / 23 removed, and every changed line is a rendered string, a snapshot of one, or the width arithmetic under one. Second-person removal throughout (`Provide your own API key` -> `Provide an API key`, `Powered by your ChatGPT account` -> `the ChatGPT account`, `Uses your plan's rate limits` -> `the plan's`, `Decide how much autonomy you want to grant Codex` -> `how much autonomy Codex gets`). The two lines that carry a factual claim are preserved exactly: `Paste or type your API key below. It will be stored locally in auth.json.` -> `Paste or type the API key below. It is stored locally in auth.json.` STILL NAMES `auth.json` AND STILL SAYS LOCALLY; and `Codex will use usage-based billing with your API key.` -> `Codex bills usage against this API key.` still says usage-based billing. The 1 credential hit is the phrase `API key` in those user-facing sentences, not a read. \u26a0\ufe0f The test change was checked rather than accepted: the DIM-modifier assertion moves from 60 cells / 35 dim to 59 / 34 because the lead-in lost exactly one column when `Uses your plan's` became `Uses the plan's`; the hyperlink's own width is unchanged, which is what the assertion exists to pin, and it is still an exact-vector equality rather than a loosened one. No credential is read, stored, rendered or transported differently. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/boot_scene.rs",
      "blob": "612033e935d67d42dd8eacb41114716987a43ea4",
      "reason": "52 added / 15 removed. One COPY comma, and otherwise a COLOUR-TOKEN correction with a long docstring: the boot screen's four colours were each a near miss of a `theme::Palette` token (ground, dim, bright, red), and the LIGHT ground `#F1EFE9` was the cream this repo had already replaced, so the first screen a new user saw used a colour nothing else in the product uses. The four `Color::from_u32` values move to the token values, `bone`/`ghost`/`ink` become `pub` and gain `lily()`, and the literal `0xC9_1A_0C` is removed from FIVE call sites - one that painted the flower and four assertions that searched the buffer for it - all now asking `BootPalette::Dark.lily()`. That is the one-owner move again: five owners of a colour is four chances to break a test's subject silently. The docstring states out loud WHY the values are still written here rather than imported (`boot_scene` is in the `estelle_tui` library, `theme` is in the `estelle` binary, so the module cannot see it) and names the binary-side test that asserts each equals its token. The 4 credential hits are the word `token` used in the DESIGN-TOKEN sense throughout that docstring. Colours and dither only; no state, input or IO. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/test_gallery.rs",
      "blob": "907d4954369ce7e6bf7048bc4f21ee05377270f2",
      "reason": "25 added / 1 removed, and it is the ONLY file in this batch with a non-zero filesystem axis, so it was resolved before this row was written. The added `write_contracts` calls `fs::write(output.join(\"contracts.tsv\"), manifest)`. THIS IS TEST-ONLY CODE: `main.rs:61-62` declares `#[cfg(test)] mod test_gallery;`, so neither the module nor the write exists in the shipped binary. Its single caller is inside `#[cfg(test)]` in main.rs and runs only when `ESTELLE_ACTUAL_GALLERY_DIR` is set (`main.rs:8456`); with the variable unset the caller is `None` and nothing is written. The directory is chosen by the operator running the suite, and the file written is a two-column TSV of screen name and contract string. The pre-existing `write_frame` in the same module already did `fs::create_dir_all` + two `fs::write` calls under the same gate, so this adds a third artifact to an existing test-output primitive, not a new one. The one removed line is `output` -> `format!(\"{}\\n\", output.trim_end_matches('\\n'))`, normalising a trailing newline. Purpose: the design book's SHIPPED/DESIGN badge is now derived from `design_book::BookScreen::contract` instead of hand-typed into HTML, which removes a second owner rather than adding one. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 1 FILESYSTEM WRITE DISCLOSED AND SCOPED ABOVE, 0 environment reads in this file, 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/tests/plan_mode.rs",
      "blob": "03b57a4f98f0e56782b831a9822cad3fbc4c6456",
      "reason": "TEST-ONLY, 69 added / 7 removed, and the additions are overwhelmingly comment. Three `personality: Some(Personality::Pragmatic)` fixture clauses are removed and replaced by a comment explaining why (`personality` is in `commands::DROPPED_COMMANDS` on a founder decision of 2026-08-07, and the submit path at input_submission.rs:332-335 filters it to None regardless), so an upstream rider stops riding on tests about collaboration mode. Then upstream's `user_turn_includes_personality_from_config` - which asserted the OPPOSITE and had been RED since the `50edb00a7` import, unseen because the lib suite aborted ~136 tests in - is INVERTED to `dropped_personality_never_reaches_the_wire` rather than deleted, so the drop stays guarded, with the feature flag forced ON so it cannot pass for the wrong reason. A second new test `no_shipped_model_advertises_personality_support` asserts the premise separately and carries its own VACUITY GUARD (`TEST_MODEL_PRESETS` non-empty before concluding anything from an empty filter). A pair that cannot both be true silently is stronger than the single test it replaces. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/chatwidget/tests/popups_and_settings.rs",
      "blob": "d48372881f45d786ad62f44e58f0be223a5a1f82",
      "reason": "TEST-ONLY, 44 added / 6 removed. One copy needle (`This plugin is disabled by your workspace admin.` -> `Disabled by the workspace admin.`), and one snapshot test replaced by explicit assertions: `personality_selection_popup_snapshot` had been RED since the `50edb00a7` import and never seen, and its failure mode is the dangerous one - with the picker refusing, `render_bottom_popup` returns the COMPOSER, which is a plausible-looking blob to snapshot, so a regenerated `.snap` would have gone GREEN ON THE WRONG SURFACE. It becomes `personality_picker_refuses_and_names_the_model`, asserting on the refusal TEXT (it names the model `gpt-5.4`, it says what it refuses) plus a negative assertion that no picker was pushed underneath, and it advances past `is_session_configured` first so the refusal under test is the MODEL refusal rather than the startup one. Replacing a snapshot with named assertions removes a green-on-anything failure mode. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/wrapping.rs",
      "blob": "e6b42a30345de5985cc1b1cc6103afbc887b12bd",
      "reason": "11 added / 16 removed - a NET DELETION that removes a duplicated table. `is_bullet_or_glyph` had six box-drawing corners hand-copied into its `matches!` arm; the same set was hand-copied into three files and THIS COPY HAD ALREADY DRIFTED, listing six of the nine, so a lone `\u2524`, `\u252c` or `\u2534` was treated as content rather than as a glyph. The six literals are deleted and the question is delegated to `crate::box_glyphs::is_lone_box_corner(raw)`, the single owner. `\u2502` and `\u2506` stay spelled out here because they are verticals, not corners, and the plain-text bullets (`-`, `*`, `+`, `\u2022`, `\u25e6`, `\u25aa`, `>`, `|`) are unchanged. The added docstring records the second reason for the delegation: a literal corner written in this file is indistinguishable, to a source scanner, from the rendered connector such a scanner exists to ban. The 2 credential hits are the word `token` in that docstring, used in the LEXER sense (`A token that is only a list bullet ...`). Pure predicate over `&str`; no IO of any kind. Mechanically 0 new URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads and 0 credential reads."
    },
    {
      "path": "tui/src/ground_block.rs",
      "blob": "e474e88a82e9f65aa68f3e8fc91b9f3c46163fba",
      "reason": "NEW FILE, 581 added, and the LARGEST NEW CAPABILITY in the undeclared set \u2014 read line by line and disclosed in full. It is the half of the grounding hook that can REFUSE an edit, plus the freshness signal that decides whether refusing is safe. NEW ENVIRONMENT READS (2, both production, neither a credential): `ESTELLE_HOOK_BLOCK` (`blocking_enabled`, matched case-insensitively against exactly `1`/`true`/`on` after trimming, so the gate is OFF by default and OFF for any other value) and `ESTELLE_REINDEX_STAMP` (`stamp_path`, a test seam; production falls back to `~/.estelle/reindex-stamp.json` via `dirs::home_dir`). NEW FILESYSTEM WRITES (2, both production, both inside `mark_indexed_at`): `std::fs::create_dir_all(parent)` and `std::fs::write(path, body)` against that one stamp file, whose content is a `{repo: unix_seconds}` JSON map \u2014 repository names and timestamps, no code, no credential. It is DELIBERATELY the same file and the same shape the Python twin already writes (`estelle_hook.py::_stamp_path`), because two hooks with two freshness signals would block on one host and pass on the other for the same edit. The other 14 filesystem hits in the axis scan are all inside `#[cfg(test)] mod tests` (tempdirs and fixture files). Filesystem READS: `fs::read` on the stamp and `fs::read_dir` in `newest_source_mtime`, which is BOUNDED before it is taken \u2014 `FRESHNESS_MAX_ENTRIES = 20_000` and `FRESHNESS_DEADLINE_MS = 2_000`, symlinks not followed, and exceeding either bound returns `None` meaning CANNOT ANSWER, which resolves to stale, which resolves to advisory. 0 URL or host literals, 0 network calls, 0 process/exec, 0 credential reads (the single hit is the module docstring saying `no socket, no credential`). \u26a0\ufe0f THE CAPABILITY, NAMED PLAINLY: with `ESTELLE_HOOK_BLOCK` set to `1`/`true`/`on`, `flagged_outcome(true, true)` returns `Blocked` and the shipped hook can now deny a customer's edit \u2014 something the runner could not do at the audited commit. Every other path degrades to advisory: not opted in, index behind, unreadable stamp, unwalkable tree, bound reached. The module's own docstring states the residual hole (a repo-level stamp records that a reindex was POSTED, not that the index holds this file's current content) and says that is exactly why the refusal stays opt-in. Wired from `top_level.rs`, which carries its own review row. \u26a0\ufe0f SCOPE LIMIT: this row attests the module's primitives, bounds and defaults. Whether the freshness proxy is good enough to refuse on is a product judgement the module argues in its own docstring and does not settle."
    },
    {
      "path": "tui/src/mcp_tool.rs",
      "blob": "c6560a4eaf786a81160e26ae6b032e8536b6adfa",
      "reason": "NEW FILE, 214 added, and A NEW NETWORK CALL SITE \u2014 the only one in the undeclared set, disclosed rather than counted as zero. `call()` does `client.post(estelle_client::Endpoint::Mcp, ...)` with a `tools/call` JSON-RPC body. IT HARDCODES NO HOST: the destination is whatever the already-audited `estelle_client::Client` was constructed with, the route is the already-declared `Endpoint::Mcp` variant, and the credential is the one the client already holds \u2014 no new credential source, no new URL or host literal (0 in the scan), no process/exec, no filesystem write, no environment read. The request adds one field the caller cannot forget (`repo`), and every text path out of the module runs through `estelle_client::mask_secret` \u2014 `Outcome::lines`, `classify`, and all four `text_of` error branches. Its reason to exist is a REFUSAL-vs-ANSWER type: `serve/mcp/__init__.py:1174` returns the graph-currency refusal as ordinary 200-OK text beginning `CANNOT ANSWER: `, and `production_hud` was splitting exactly that sentence into three rows of chokepoint filenames. The prefix is defined from the server rather than invented, its trailing space included, and a negative-control test proves a sentence merely MENTIONING the phrase still classifies as an answer. \ud83d\udd34 FINDING RECORDED FOR THE FOUNDER, NOT FIXED HERE: `docs/egress-sinks.toml` maps sinks by `source_file`, and no row names `tui/src/mcp_tool.rs` or its caller `tui/src/production_hud.rs`. The DESTINATION (api.fatelabs.ca) is already declared several times over, so nothing new leaves the machine \u2014 but the census's source-file attribution is now incomplete for this call path. `verify_egress` does not check that every call site is declared, so the audit passes either way. Left for a deliberate decision because adding a sink row means bumping `released_sink_count`, which is a claim about the census and not mine to make silently."
    },
    {
      "path": "tui/src/design_book/script.rs",
      "blob": "edbbfb66ccd4aafa91c14294533e1c9503333987",
      "reason": "NEW FILE, 443 added, and it CONSTRUCTS AN `estelle_client::Client` \u2014 the one hit that had to be read before this row could be written. `dress()` is `pub(crate)`, not test-gated, and its FIRST statement after `auth_resolved` is `if !fixtures { return; }`, so nothing below it runs on a default-configuration process. Inside that gate it builds `ApiKey::new(\"estelle_demo_fixture_key\")` and `Client::new(\"http://127.0.0.1:9/\", key, MINIMUM_TIMEOUT)`. Port 9 is the TCP DISCARD port on loopback: the client is constructed and never dialled, and the reason it must exist at all is that `live_renderer.rs:1492` short-circuits to `Live Monitor unavailable. / Run /login here.` when `client` is `None`, before it reads anything else. It REPLACES whatever client the app had for the duration of a demo, which narrows egress rather than widening it. The 2 URL/host hits are that loopback literal and the fixture email `you@saltbox.dev`; the 12 credential hits are the fixture key, the film's credential-fence beat and the prose about both. 0 real network destinations, 0 process/exec, 0 filesystem writes, 0 environment reads. The rest of the file is film data plus `#[cfg(test)]` guards, two of which are load-bearing: one asserts the fixture key is STILL REFUSED by the shipped `estelle_client::find_secret_shape` (so the beat is a real refusal rather than a drawing of one), and one asserts the refusal's own wording survives `mask_secret` while the key itself is still masked \u2014 a positive and a negative control on the same redactor."
    },
    {
      "path": "tui/src/design_book/mod.rs",
      "blob": "f9b0b5d4a1773e95233b3f6e6b1c0ff34847b816",
      "reason": "NEW FILE, 631 added: the design book's screen registry and, more importantly, THE ONE OWNER OF \"MAY FIXTURE DATA BE DRAWN\". 1 production environment read \u2014 `ESTELLE_DEMO_FIXTURES` in `fixtures_allowed`, compared to the exact string `1` rather than tested with `is_some`, so a variable exported as `0` or empty does not open the gate. The other 8 environment hits are `set_var`/`remove_var` inside one `#[cfg(test)]` test that restores what it found. 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes; the 6 credential hits are prose plus the test `no_book_screen_renders_a_credential_or_a_fragment_of_one`, which FORBIDS six vendor prefixes (`sk-`, `sk_`, `estelle_live_`, `ghp_`, `github_pat_`, `xoxb-`) across every screen \u2014 the module previously shipped `sk-ant-\u20264f2c` under a footnote claiming it printed no value. `render()` is the only function the product calls and it returns the honest empty state unless the gate is open; `screen.render` stays private to the module. The paired test asserts BOTH directions \u2014 every screen's needle is absent with the gate shut AND present with it open \u2014 so the absence check cannot pass on a renderer that draws nothing."
    },
    {
      "path": "tui/src/box_glyphs.rs",
      "blob": "7e37597cca8b4717b2187e141d433d9565ba7042",
      "reason": "NEW FILE, 547 added, of which the SHIPPED code is nine lines: `BOX_CORNERS` (nine chars) and `is_lone_box_corner`. Everything from line 49 down is `#[cfg(test)] mod source_guard`, a scanner that walks this crate's own `src` (`env!(\"CARGO_MANIFEST_DIR\")`, a COMPILE-TIME macro, not a runtime environment read) with `std::fs::read_dir` and `read_to_string` and fails on a box corner in code that ships. Those are filesystem READS in test code; 0 filesystem writes, 0 network, 0 process/exec, 0 runtime environment reads, 0 credential reads (the 4 hits are the word `token` used in the LEXER sense plus a comment about the `cfg(test)` gate token). The 1 URL hit is `\"https://x\"` inside a scanner fixture proving a `//` inside a URL is not a comment. Every glyph in the file is written as a `\\u{\u2026}` escape so the guard does not find itself and therefore needs no exemption list \u2014 an assertion with a carve-out being where the next box hides. It closes a measured hole: `main.rs`'s existing check reads rendered GALLERY FRAMES and was blind to fifteen live production sites emitting `\u2514` from widgets that build their own lines."
    },
    {
      "path": "tui/src/demo_session.rs",
      "blob": "ceb999156758a3e27fae2bc9d96f5ae9e4458c0b",
      "reason": "NEW FILE, 714 added: the `estelle demo --session N` player. The single process hit is `use std::process::ExitCode` \u2014 the RETURN TYPE of `run`, not an exec: 0 `Command::new`, 0 `spawn`. Also 0 URL or host literals, 0 network calls, 0 filesystem writes, 0 environment reads, 0 credential reads. It draws nothing of its own by design: it constructs a real `App`, puts scripted text in the real composer, pushes real `TranscriptEntry` values and calls `live_renderer::render_frame` on `frame.area()`, so the film is the product rather than a picture of it. What it owns is TIME \u2014 a cue sheet of `(millisecond, mutation)` \u2014 and every loop is bounded: `ceiling` is derived from the cue sheet and clamped to `MAX_FILM_MS * 4`, the boot wait has its own `BOOT_CEILING`, and a `speed` that is not finite or is at or below 0.05 is replaced by 1.0 at the boundary. `apply` only ever pushes to or grows the transcript \u2014 no clear, no truncate, no assignment \u2014 and keys ABORT rather than advance. Fixture data reaches it only through `script::dress(.., fixtures)` and `design_book::render`, both behind the one gate in `design_book/mod.rs`."
    },
    {
      "path": "tui/src/demo_session_tests.rs",
      "blob": "0d5831d9de967e39dd7d3848a5d0772b7bdbd3e7",
      "reason": "NEW FILE, 655 added, TEST-ONLY: attached with `#[path]` as a CHILD of `demo_session` so its guards can reach that module's private `Cue` machinery without widening it to `pub(crate)` \u2014 the transcript stays out of reach of code not allowed to touch it. Every frame assertion renders through `live_renderer::render_frame` on a `TestBackend` rather than through a helper of its own, which is the correction that matters: a test asserting on a frame this module composed would have passed on all seven of the founder's reported defects, because all seven were in the composing. Mechanically 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads. Four of its guards are the fixture gate's teeth from the other side: a film cannot draw fixture numbers with the gate shut, no frame says `saved`, no frame carries a box corner, and no internal id or line count reaches a frame."
    },
    {
      "path": "tui/src/design_book/session.rs",
      "blob": "b0138fddec681de099385f1fdb553a552661781d",
      "reason": "NEW FILE, 542 added: the vocabulary a scripted film is written in (`Say`, `Key`, `Beat`, `Film`) plus `table_lines`, the one function that lays a `|`-delimited block out at the REAL pane width. Pure text over `crate::cols`; the only outside call is `estelle_machine::machine()/named_model()/fit()` in `local_fleet_lines`, which measures THIS machine's hardware through a crate already in the tree and prints the library's own `estimate_notice` verbatim rather than paraphrasing a speed claim. `detected_machine` memoises that detection in a `OnceLock` because calling it per cue took the suite from 25s to 48s and pushed an unrelated 50ms frame-budget assertion over its limit. Bounds are named: `MIN_COL`, `MIN_WRAP`, `MAX_BEATS`, `MAX_FILM_MS`, `FALLBACK_PANE`, and `fitted`'s shrink loop carries an explicit guard counter. Mechanically 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads."
    },
    {
      "path": "tui/src/design_book/rail.rs",
      "blob": "8bfea7a2fb94a4746afc22f157ca420f22514154",
      "reason": "NEW FILE, 428 added: the production rail's per-frame motion. `tick()`'s first statement is `if !fixtures { return; }`, and a test asserts that with the gate shut `prod_overview` and `prod_agent_health` stay `None` at every moment of every film. Everything it writes is a `serde_json::json!` value assigned into `app.prod_*` fields \u2014 invented numbers, deterministic functions of `(film, elapsed_ms)` with no RNG so a re-shot take matches the last one frame for frame. The 3 URL hits are FIXTURE STRINGS placed into the JSON it renders: `https://{name}/health` in an uptime-check row and two `https://github.com/{repo}/pull/N` links. Nothing in this file has a network primitive \u2014 it imports only `serde_json::json` and `design_book::session::Film` \u2014 so those are text the rail DRAWS, never a request it makes. 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads. One `debug_assert!` was added because `.ok()` had silently swallowed a fixture typo (`\"issue_key\": null` against a `#[serde(default)] String`, which defaults only on an ABSENT key) and the band went dark with no error."
    },
    {
      "path": "tui/src/design_book/script_solo.rs",
      "blob": "788142436ca11de7b8f547a624dad2a2a05d2654",
      "reason": "NEW FILE, 673 added, DATA ONLY: `SOLO`, a `const &[Beat]` for film 1, plus nine `static &[Col]` column specs and `FIXTURE_KEY`. Verified there is no code in it beyond those declarations. 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads. THE ONE THING WORTH A SECOND LOOK is `FIXTURE_KEY = \"sk-ant-api03-notarealkey-demo-fixture-0000000000\"`, a credential-SHAPED literal committed to source. Characterised rather than assumed: (a) it is inert to the parent repo's Python scanners, which exempt any match carrying an example marker, and it carries two (`notarealkey`, `0000000000`), asserted by a test so losing one is not losing the property; (b) it IS caught by the Rust fence the CLI actually runs \u2014 `find_secret_shape` returns `Some((\"an sk- API key\", 1))`, asserted by another test, which is what makes the film's credential beat a real refusal; (c) I checked it against THIS AUDIT'S OWN `_FIXTURE_SECRETS` regex and it does NOT match, because `sk-[A-Za-z0-9]{16,}` needs sixteen consecutive alphanumerics and the hyphens break it at three. That is luck rather than design, and it is written here so the next reader knows the check was run. It appears in no committed `*.snap` (687 scanned, 0 offenders). The 2 URL hits are `stripe.com/docs/...` and `vercel.com/docs/...` rendered as documentation citations inside a scripted research beat."
    },
    {
      "path": "tui/src/design_book/script_cartwheel.rs",
      "blob": "5927222c81731e21f4fe0409e19cf73ecda5fa3e",
      "reason": "NEW FILE, 360 added, DATA ONLY: `CARTWHEEL`, the `const &[Beat]` for film 2, plus seven `static &[Col]` specs. No functions, no `impl`, no macros beyond the array literals \u2014 verified by reading every non-comment, non-string line. The 1 URL hit is `https://cartwheel.slack.com/archives/C0192/p1756\u2026` rendered as a fixture Slack permalink in a beat about a teammate's parked proposal; nothing in this file can open it (its only imports are `crate::cols::Col` and the session vocabulary). Mechanically 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads."
    },
    {
      "path": "tui/src/design_book/script_estelle.rs",
      "blob": "54a1dca20ebd7005905cd3a4f2bcb3bb8ef8d2c5",
      "reason": "NEW FILE, 219 added, DATA ONLY: `ESTELLE_REPO`, the `const &[Beat]` for film 3, plus six `static &[Col]` specs. Same shape as its two siblings and verified the same way. The 1 URL hit is inside a module comment explaining why the film's fixtures name a fictional company rather than `uqeu/estelle` or `khai@fatelabs.ca`; the 2 credential hits are the words `auth.rs` in a cited file path and `token` in the phrase `no token meter, ever`. Mechanically 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 real credential reads."
    },
    {
      "path": "tui/src/design_book/account.rs",
      "blob": "37c847d4350d2c82e72f634603dd377b8c7dc8a9",
      "reason": "NEW FILE, 310 added: the three ACCOUNT screens (provider keys, a failing doctor, the frecency resume list), split out of `surfaces.rs` when that file passed the 800-line hard limit. Pure `Vec<Line>` over `crate::cols` and `theme::Palette`. THE POINT OF THE FILE IS A CREDENTIAL REMOVAL: screen 30 shipped `sk-ant-\u20264f2c` and `sk-\u20269d1a` under a footnote reading *\"Estelle prints a prefix and a state, never a value\"* \u2014 a sentence that made the leak sound like the safeguard. Both fragments are gone, the footnote is rewritten to `No prefix, no suffix, no value.`, and the rule is now enforced for every screen by `design_book::tests::no_book_screen_renders_a_credential_or_a_fragment_of_one`. The 7 credential hits are that prose plus the destructured cell name `auth`; the 2 URL hits are `api.fatelabs.ca` and `khai@fatelabs.ca` inside doctor fixture ROWS \u2014 text drawn on a screen, not a destination, since the file's only imports are ratatui, cols, kit, marks and theme. 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads."
    },
    {
      "path": "tui/src/design_book/answers.rs",
      "blob": "4a7fdd157067cd362d4154e354371a8472e3aab5",
      "reason": "NEW FILE, 704 added: the four ANSWER screens (34, 39, 40, 41). Pure `Vec<Line>` builders over `crate::cols`, `theme::Palette` and \u2014 for screen 40 \u2014 the PRODUCTION `graph_view::lines` renderer rather than a second drawing of it. Zero on every axis: 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads. Its tests are the interesting half: the table's markdown separator must be no narrower than the head above it, a truncated tool output must COUNT what it hid, and the chokepoint row must carry a numeric blast radius rather than the `\u2014` that means unmeasured."
    },
    {
      "path": "tui/src/design_book/costing.rs",
      "blob": "fdcb1b848383c8304022d04cb69323a4611f1d67",
      "reason": "NEW FILE, 594 added: screens 32 and 33, the memory-remaining and spend panels. Pure line builders; zero on every axis (0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads). Every figure on the frames is a fixture, and the module docstring names the source of each one \u2014 the `/sweep/estimate` field names, the founder's own August mocks, and the ladder production serves on `/plans`. Its `bar()` is fixed-width by construction so a value cannot push the columns right of it, and `load_colour`'s three bands are pinned at their boundaries."
    },
    {
      "path": "tui/src/design_book/kit.rs",
      "blob": "1655e6a58271da5a368ecac8a67edcd011a9d79f",
      "reason": "NEW FILE, 126 added: the shared primitives every book screen is built from (`fields`, `table`, `bar`, `prose`, `status`, `rail`, `step_of`, `verdict`), extracted when `surfaces.rs` reached 961 lines against a 800-line hard limit. Nothing was generalised on the way out \u2014 every helper here already had more than one caller, because a helper invented during a split is a second owner nobody asked for. `fields::<N>` asserts the arity in BOTH directions, so a catalog row that quietly lost a `|` fails rather than rendering its note under `state`. Zero on every axis."
    },
    {
      "path": "tui/src/design_book/loops.rs",
      "blob": "3766bf5f0eb891cf834913ad69e9a19fb996fc85",
      "reason": "NEW FILE, 499 added: the three screens where Estelle refuses and then keeps going (9, 10, 11). Screen 9 delegates to `crate::gate_refusal::lines` \u2014 the same renderer the live modal calls \u2014 rather than redrawing the product's loudest moment. Zero on every axis; the single credential hit is the phrase `design token` in a comment. Its guards are worth naming because they are the founder's rules made checkable: only the MARK pulses and never the words (asserted on the PAINTED colour, `line.style.patch(span.style)`, after a mutant that styled the whole line passed a check on `span.style.fg` alone); a refusal sentence survives a narrow frame character for character; and every screen must show a step in flight AND a step still to come, so a refusal cannot be a dead end."
    },
    {
      "path": "tui/src/design_book/panel.rs",
      "blob": "73a5e20dc81f146cc30ed3481f1001aba882de48",
      "reason": "NEW FILE, 251 added: screen 33b, the costing panel. Split out of `costing.rs` at the 800-line limit, importing `bar` and `load_colour` from it rather than copying them. Pure line builder over `crate::cols`; zero on every axis. One detail read for accuracy rather than skimmed: a rule's mode text was changed from `ctrl+m locks a role` to `enter locks a role`, because Ctrl+M IS carriage return and arrives as `Enter` with no modifier \u2014 a design frame advertising a key that cannot be bound in this binary is the same defect as a caption describing a panel that is not drawn."
    },
    {
      "path": "tui/src/design_book/panes.rs",
      "blob": "d64c6eaf373541b925e603cac72d0b7306436327",
      "reason": "NEW FILE, 795 added: screens 25, 35 and 38 \u2014 the three that put more than one thing in one terminal. Pure line builders; zero on every axis. The two-pane split is a plain `\u2502` divider column placed by a `cols` spec with rules that STOP either side of it, and the test asserts the divider lands on ONE column across at least twelve rows \u2014 a hand-counted split has no owner to assert against. Its fleet table quotes `orchestra_view::MISSING_PER_WORKER_SPEND` verbatim rather than paraphrasing it, so the book cannot drift from the live table's own disclosure that per-worker model and cost have no server contract, and a test checks the fleet summary is the arithmetic sum of the rows above it."
    },
    {
      "path": "tui/src/design_book/skills.rs",
      "blob": "d8a64e4408a28ea43f2fa16df5bd68885a32cf35",
      "reason": "NEW FILE, 501 added: screens 12\u201314, the skill surface. Pure line builders; zero on every axis (the 4 credential hits are the words `token cost` and `auth module` in fixture copy). Its guards pin two founder rules: the selected row is highlighted with `palette.tint` and never framed (asserted together with the corner ban, so deleting the highlight to fix a box fails), and the offer screen may not contain `running` or `applied`, because this frame is drawn before anything has been sent and past-tense wording would describe a system that auto-fires."
    },
    {
      "path": "tui/src/design_book/stats.rs",
      "blob": "0059073a3eeea0060da7ea1499949de6282eb752",
      "reason": "NEW FILE, 447 added: screen 42, where the run's time and tokens went. Pure line builders; zero on every axis (the 2 credential hits are `first token` as a latency phase name and the same phrase in the docstring). It is the honest half that makes it worth reading: the screen carries four `SOURCES` rows saying which of its own numbers were counted \u2014 one `measured` (the cache split, a real `TokenUsage` field) and two `NOT measured` (the per-phase clock and the per-tool tally, neither instrumented in this client) \u2014 and a test asserts the string `NOT measured` appears exactly twice, so a screen that said it about the one measured number would fail. The time table also states in prose that its rows do not sum to wall clock, because reasoning is counted inside generation."
    },
    {
      "path": "tui/src/design_book/surfaces.rs",
      "blob": "c2b4e22a3e6664893cf398c8834062fc9feb129e",
      "reason": "NEW FILE, 581 added: the login, no-repository, command-audit and shell-mode screens. Pure line builders; zero on every axis. The 2 URL hits are the fixture email `khai@fatelabs.ca` in login rows and the 4 credential hits are the words `credential` and `subscription` in copy about which login pays for model tokens \u2014 no key, prefix or value anywhere, which the shared `no_book_screen_renders_a_credential_or_a_fragment_of_one` test now enforces across every screen including these. Its command-audit screen is measured rather than drawn: it cites `commands.rs:62` and `commands.rs:285` for `logout` being advertised and dropped, and a test asserts the command and its description never touch \u2014 the shipped `/help` pads to twelve and four command names are exactly twelve characters long."
    },
    {
      "path": "tui/src/gate_refusal.rs",
      "blob": "2f5c15d8f332c2cbcc5315d11febbcbdad956f09",
      "reason": "NEW FILE, 395 added: the single `Gate refused` renderer, called by both `screens.rs` and `live_renderer::render_gate_modal`, which had been drawing two different pictures of the product's loudest moment. Pure `Vec<Line>` over `crate::cols` and `theme::Palette`. THE 3 NETWORK HITS ARE ALL FALSE POSITIVES and were read individually: each is the STRING `\"reqwest::Client::retry()\"`, used as a fixture claim \u2014 a hallucinated API the gate refuses \u2014 in one test's data and two of its assertions. There is no `reqwest` import in the file. 0 URL or host literals, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads. Its own rule is that a refusal may never be truncated, and the test proves it by reassembling the rendered rows at four widths and comparing them word for word against the input."
    },
    {
      "path": "tui/src/graph_view.rs",
      "blob": "b6fe75552b22e3a12ba75d0a2de3056d5d07f282",
      "reason": "NEW FILE, 636 added: the walkable code-graph renderer, shared by the live pane and design-book screen 40. Zero on every axis. It takes already-fetched data \u2014 it has no client and no transport \u2014 and runs `estelle_client::mask_secret` over the server's refusal text before drawing it. Three states rather than two: a walk, and `Surface::Withheld`, which gets the WHOLE pane rather than a footnote under an empty table, because an empty risk map reads as no risk and the server said neither. `Node::moves` distinguishes `None` (not asked) from `Some(0)` (asked, nothing depends on it) and draws `\u2014` for the first, which is the absent-versus-zero rule this repo has paid for repeatedly. Its module docstring records the measurement that shaped it: `fan_out` exists nowhere in the server and `fan_in` exists once, file-level, never reaching a tool reply \u2014 so the two columns that used to be drawn were a granularity the graph does not have."
    },
    {
      "path": "tui/src/orchestra_view.rs",
      "blob": "e82cdf6b27f6f413d294d5917438907f5553d607",
      "reason": "NEW FILE, 560 added: the Orchestra worker table, shared by `screens.rs` and `live_renderer.rs` so the catalog and the live session cannot draw different tables. Zero on every axis; it renders an already-fetched `FleetSnapshot` and has no transport. Every server-supplied string goes through `estelle_client::mask_secret` \u2014 the narrator text and each agent's `current_action`. The cost column is `\u2014` on every row and `MISSING_PER_WORKER_SPEND` is printed under the table, because `FleetAgent` carries neither a model nor a cost; filling those cells from the fleet's plan floor divided by worker count would be a fabricated number in the interface whose job is refusing them. `last_seen` returns the literal string `clock ahead` for a row dated ahead of this client's clock rather than `0s`, which would claim an observation that did not happen."
    },
    {
      "path": "tui/src/session_view.rs",
      "blob": "b3e773f181b66dc69125463b3882b917afe16e59",
      "reason": "NEW FILE, 350 added: the live session frame's geometry \u2014 the `session \u2502 production` split, the divider, and the design's rule headings. Zero on every axis. It exists because `cols` was the one piece the redesign never wired: `screens.rs` rendered thirteen pages on it while the live TUI referenced `cols` zero times, so the catalog shipped one design language and the customer's terminal drew another. `split()` refuses rather than squeezing \u2014 below `46 + 2 + 1 + 2 + 30` columns it returns `None` and the rail is dropped \u2014 and the areas and the divider are derived from the same spec, so they cannot disagree about where the split is. `design_rule` trims an over-long mode at the frame's known width rather than inside the shared primitive, whose other callers pass a page width they have already sized for."
    },
    {
      "path": "tui/src/marks.rs",
      "blob": "9424d6fe91cb80b9445407c24f400191506b918a",
      "reason": "NEW FILE, 393 added: the status glyph vocabulary and the one rule about pulsing it. Zero on every axis. `headline()` is the only sanctioned way to put a pulse beside words, and it applies the pulse to exactly one of the two spans, so a caller cannot pulse the TEXT through this module even by accident \u2014 five call sites were doing exactly that. The docstring records the defect that motivated the file: it said `five marks` for months while the product drew six, `?` being a bare literal in two shipped screens, in neither enum and in neither test. `Mark::ALL` now has one owner that every property test iterates, so a seventh mark added without a test is a compile error rather than a silent gap."
    },
    {
      "path": "tui/src/sweep_estimate.rs",
      "blob": "9a763915e57ccf5cb645f9230c607ded8666d3c4",
      "reason": "NEW FILE, 604 added: renders the whole `POST /sweep/estimate` reply, which the client previously read one field of (`fits`). Zero on every axis \u2014 it takes a `serde_json::Value` that the caller already fetched; 0 URL or host literals, 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads. All 10 credential hits are the word `token` used to mean a TOKEN COUNT, which is this module's whole subject: `top_level::concise_value`'s `sensitive_key` guard drops any key whose name contains `token`, so `estimated_tokens`, `net_new_tokens`, `held_tokens`, `remaining_tokens`, `blocked_tokens` and `billable_tokens` were all struck out as if they were credentials and the user was told a sweep did not fit with no number and no sentence. \u26a0\ufe0f DISCLOSED RATHER THAN GLOSSED: this module is an ALLOWLIST that deliberately BYPASSES that name-based filter. It names every field it prints \u2014 repo, six token counts, `fits`, `overflow_cost_usd`, `suggested_plan`, `largest_paths`, `exact` and the server's written `message` \u2014 so a field nobody designed for cannot reach the terminal, but it does NOT run `mask_secret` over the server's `message` the way `graph_view` and `orchestra_view` do over theirs. The named fields are counts, repo-relative paths and server-authored prose about capacity; none is a credential shape. Recorded so the next reader knows the difference was noticed rather than missed."
    },
    {
      "path": "tui/src/run_spend.rs",
      "blob": "c024e40aa3905565f1161dc21bbcd37c9c7269c4",
      "reason": "NEW FILE, 535 added: the per-model run-spend breakdown, reading `usage_breakdown` \u2014 which the server has serialised onto every `/work` result all along and which `grep -rn \"by_model\\|est_cost_usd\"` found zero readers for in the client. Zero on every axis; it formats an already-fetched `serde_json::Value` and has no transport. All 4 credential hits are the word `token` meaning a token COUNT. Three honesty properties are enforced rather than asserted in prose: `price_known: false` ships `est_cost_usd: null` and prints `\u2014` rather than `$0.00`; a total that excludes an unpriced model says so by name; and the word `saved` never appears \u2014 the `savings` block is rendered as `baseline (modelled)` and a `difference`, with the server's own assumption sentence printed under it, because a modelled counterfactual is not money saved. It deliberately does NOT print a run total: `render_work_completion` owns that and is pinned as the last line of a work reply, and a second total derived here would disagree with it the day either derivation moved."
    },
    {
      "path": "tui/src/plain_english.rs",
      "blob": "5f75bd9519b0fd8b89a7a371a4e10789ae10e367",
      "reason": "NEW FILE, 526 added: the copy-voice meter and gate over the four trees this crate inherited from Codex. The 1 environment hit is `env!(\"CARGO_MANIFEST_DIR\")`, a COMPILE-TIME macro, and both filesystem calls (`read_dir`, `read_to_string`) are inside `#[cfg(test)] mod scan` \u2014 reads of this crate's own source, never writes. The 2 URL hits are `starts_with(\"http://\")` and `starts_with(\"https://\")` in `is_prose`, which EXCLUDE a URL from being judged as prose; they are guards, not destinations. 0 network calls, 0 process/exec, 0 filesystem writes, 0 credential reads. It states its own limit in the module docstring \u2014 it reads string literals, so it cannot see copy assembled at runtime, copy from the server, or a banned word living in a `format!` argument \u2014 and it carries three controls: a floor on files and prose literals read (a scan that read nothing reports zero findings, which reads exactly like a pass), a positive control firing on the sentences the founder named, and a negative control staying silent on copy that is already right. Its one exemption carries a written reason AND a test asserting the string it protects still exists."
    },
    {
      "path": "tui/src/release_version.rs",
      "blob": "f788913ff7e0f9a4e6556186533ac4afb5cd4248",
      "reason": "NEW FILE, 112 added, ENTIRELY `#[cfg(test)]` \u2014 the module contains one test module and nothing else. Its 1 environment hit is `env!(\"CARGO_PKG_VERSION\")` and its three file reads are `include_str!`, all COMPILE-TIME macros against in-repo manifests; 0 runtime IO of any kind on any axis. It asserts the four in-tree owners of \"what version is this CLI\" agree: the binary, `npm-shim/package.json`, `estelle-plugin/.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` \u2014 the last read by anchoring on the `plugins` array first, because that file carries TWO `version` keys and the first draft read `metadata.version` and reported a mismatch that did not exist. It carries a vacuity guard on the parsed values (a slightly-wrong parser returns `\"\"`, which would make every comparison below it meaningless) and its docstring names the half it does NOT cover: only a read-back from npm and GitHub can say what was actually published."
    },
    {
      "path": "tui/src/test_stack_budget.rs",
      "blob": "19c4669fa2e3ccd3ceccb4e2078858f0ba02fe5c",
      "reason": "NEW FILE, 58 added, ENTIRELY `#[cfg(test)]` apart from one `const`. Its 1 environment hit is `std::env::var(\"RUST_MIN_STACK\")` inside the test; 0 network, 0 process/exec, 0 filesystem, 0 credential reads. It asserts the 8 MiB worker-stack floor REACHED THIS PROCESS, which is the part that was actually broken \u2014 the same number was already written down twice (the Windows `/STACK` link args and `release.yml:88`) and still did not hold for a developer running the suite locally. The defect it guards is the one this repo cares about most: below the floor `cargo test -p estelle-tui --lib` does not fail, it SIGABRTs partway with no `test result:` line at all, and an absence reads as a pass \u2014 which is why every recorded baseline said 154 tests while 3,251 had never run. Its docstring states the limit: it asserts the CONFIGURED budget, not the achieved one, and a future fixture needing 9 MiB will overflow with this test still green."
    },
    {
      "path": "tui/src/affinity_cli.rs",
      "blob": "8dec67539fed22810e1d2794ee9a527b4db09687",
      "reason": "NEW FILE, 75 added: a three-variant surface enum dispatching render and key handling to `costs` and `models`. Zero on every axis. Two items carry `#[allow(dead_code)]` with a WRITTEN REASON rather than a bare suppression: the affinity MODELS surface has no key or command to reach it, because its only door was `ctrl+m` \u2014 which is carriage return in this binary \u2014 and it was removed rather than moved while the replacement binding is an open founder ruling. The code is kept rather than deleted so the ruling is one binding away, and the reason is on the suppression where a reader will find it."
    },
    {
      "path": "tui/src/affinity_cli/costs.rs",
      "blob": "03044df4a9d68203d495a2a5a0e74b949cdea405",
      "reason": "NEW FILE, 663 added: the spend ledger screen. Zero on every axis \u2014 it reads `CommandReply` values the caller already fetched and renders them; no transport, no filesystem, no environment. The 1 credential hit is the label `Latest Estelle token invoice`. Every list it walks is bounded by a named constant before it is taken (`MAX_RECEIPT_ROWS`, `MAX_LIVE_MODELS`, `MAX_WORKERS`, `MAX_CALLS_PER_WORKER`, `MAX_RECEIPT_STAGES`). Its `Money` enum keeps five states apart \u2014 `Exact`, `Upper`, `Lower`, `Partial` and `NotMeasured` \u2014 and only `Exact` contributes to the session total, so a ceiling or a floor marks the session INCOMPLETE rather than being summed as if it were measured. The footer says `no savings claim is made` and the summary says vendor-list estimates are not Estelle charges or provider invoices."
    },
    {
      "path": "tui/src/affinity_cli/models.rs",
      "blob": "9d003282fa150a29e896c8927b521066757aa816",
      "reason": "NEW FILE, 437 added: the per-role model-pin screen. Zero on every axis. It BUILDS a request body (`begin_save` / `request_body` return a `serde_json::Value`) but does NOT send one \u2014 there is no client and no transport in this file; the caller owns the wire, and that caller has its own review row. `from_replies` reads the server's effective display table and refuses to offer a model the account has not configured, and `apply_saved` validates the reply rather than assuming it. Four bounds are named constants (`MAX_VISIBLE_OPTIONS`, `MAX_ROUTING_ROWS`, `MAX_PROVIDER_ROWS`, `MAX_MODELS_PER_PROVIDER`). A test asserts the proposed table is COMPLETE and carries no private routing key."
    },
    {
      "path": "tui/src/answer_currency_tests.rs",
      "blob": "2519a49646b20c94e8e1186b9a83af03a499b482",
      "reason": "NEW FILE, 169 added, TEST-ONLY: three tests attached to `main.rs`, asserting on the RENDERED BUFFER through `render_frame` on a `TestBackend`. Zero on every axis. They make a design-book claim checkable from this side \u2014 screen 10 declared `no CLI surface reads the staleness verdict yet`, while the server had been attaching a `code_currency` block to `/memory/chat` all along \u2014 and they assert both directions: a decertified answer leads with the verdict, and a CURRENT index draws no currency row at all, so the positive test cannot pass on a renderer that decorates every answer."
    },
    {
      "path": "tui/src/composer_band_tests.rs",
      "blob": "3c405a26009ac75709bc06640699502ebb8fe02b",
      "reason": "NEW FILE, 130 added, TEST-ONLY: two tests asserting on the RENDERED BUFFER's cells rather than on the code that builds it. Zero on every axis. The defect they pin could not have been found by reading: `transcript.rs` builds ONE background and no padding, but `history_cell` frames every user turn with a blank row at each end and `public_widgets::history_transcript` re-bands those blanks too \u2014 two files each doing something reasonable and a band nobody wrote. So the test counts maximal runs of lit rows in the buffer and asserts there is exactly one and that no blank row is lit, with a negative control asserting a frame carrying no user turn lifts no row at all."
    },
    {
      "path": "tui/src/hook_guard.rs",
      "blob": "bd218140e6611a04463af02a9a640246c8f4faaa",
      "reason": "524 added / 39 removed, and the largest modification in the undeclared set. It is a SHELL-STRING matcher and stays one: no new dependency (`regex_lite::Regex` was already the mechanism at the audited commit), 0 network calls, 0 process/exec, 0 filesystem writes, 0 environment reads, 0 credential reads \u2014 the 6 credential hits are the words `secret`/`token` inside the human-readable REASON strings for the two clauses that ban `railway variables`, and the 1 URL hit is a comment. The change inverts the `rm` rule: the old clause enumerated catastrophic paths (`/`, `~`, `$HOME`, `/etc`, `/usr`, `/Users`) and `rm -rf ~/Desktop` walked straight through it in silence, as did `~/Documents`, `~/.ssh`, `./src` and `../sibling-repo`. Now a recursive force-delete warns UNLESS every target is a regenerable build artifact (`DISPOSABLE`) or under a scratch root (`SCRATCH`), which is the defensible direction: those two sets are finite and the set of paths that matter is not. The table also grows the destructive-git family (`checkout --`, `restore`, `reset --hard`, `clean -fd`, `branch -D`, `stash drop|clear`, force-push except `--force-with-lease`), destructive SQL, infrastructure teardown, publishing, and this repo's own hard rules (`railway variables`, bare `railway up`, `history -c`). EVERY BRANCH IS STILL ADVISORY \u2014 `dangerous_command` returns `Option<&'static str>` and the call site at `top_level.rs:494` names the risk and lets the command proceed; nothing here blocks. Bounds are named (`MAX_RM_TARGETS`, `MAX_RM_FLAGS`, `MAX_RM_WORDS`, `MAX_GIT_WORDS`) and a capped read returns `UNREAD_TARGETS` \u2014 a DIFFERENT string from the ordinary warning \u2014 so padding a line with 32 copies of `node_modules` cannot buy silence for the path after them. The module docstring states the limit out loud: an alias, a variable, a wrapper script, a heredoc or a `$(\u2026)` substitution all defeat it, and nothing here changes that."
    },
    {
      "path": "tui/src/style.rs",
      "blob": "8b5ae68a9f32882475550a7fbfd0a4f2494639c0",
      "reason": "445 added / 8 removed. Two changes. (1) PRODUCTION, 6 lines: the accent's two hardcoded values become named constants read from the product's own palette \u2014 `DARK_ACCENT` (0x7FB3C8) replacing bare `Color::Cyan` and `CREAM_ACCENT` (0x264B5E) replacing `LIGHT_BG_ACCENT_RGB`, which is why the `hooks_browser_view` test in this same batch had to stop pinning `Color::Cyan`. (2) EVERYTHING ELSE is `#[cfg(test)] pub(crate) mod brand_palette_guard` (gated at the line above its declaration), a source scanner with per-file BUDGETS for named-colour call sites. Its filesystem calls are `read_to_string` and `read_dir` \u2014 READS of this crate's own `src`, rooted at the compile-time `env!(\"CARGO_MANIFEST_DIR\")`; 0 filesystem writes, 0 network, 0 process/exec, 0 runtime environment reads, 0 credential reads (the 1 hit is the word `token` meaning a design token, and the 2 URL hits are `\"https://\u2026\"` fixtures proving the comment stripper does not treat `//` inside a string as a comment). The guard states its own limit: a per-file MAX cannot see a SWAP \u2014 one removed from file A and one added to file B nets zero \u2014 so it catches growth and new doors, which is what it claims and all that it claims. It carries a positive control (a string that DOES contain the drift is seen) and a negative one (the same text as a comment is not), and a non-ASCII case so the char scanner cannot panic on this crate's source."
    },
    {
      "path": "tui/src/agent_loop.rs",
      "blob": "f3531a091d4dab6af5f5d794b7b86e2c04748c66",
      "reason": "new file authored by us, not derived from upstream codex. The bounded /loop primitive: pure decision logic with NO network, NO filesystem writes and NO process spawns anywhere in the file (grepped for reqwest/http/Url/hyper, fs::write/File::create/remove_file, Command::new/process:: - all zero), so it adds no egress and no new capability. Its bounds are named constants clamped at construction (MAX_LOOP_ITERATIONS 12, MAX_LOOP_WALL_CLOCK 4h, MAX_LOOP_TURNS 24), it carries a fail-closed allowlist whose completeness is test-enforced against the whole command catalogue, shell steps are refused outright, and self-arming is default OFF and session-scoped. A loop cannot arm a loop."
    },
    {
      "path": "tui/src/plugin_currency.rs",
      "blob": "1c827be257f6f2115b8ee03cbc8e72581d5b874d",
      "reason": "new file authored by us, not derived from upstream codex. Tells the user when their installed PLUGIN manifest is behind the published release. It adds NO new egress sink: it reads the already-declared version_check cache (crate::version_check, GitHub releases, 24h TTL) and introduces no HTTP client of its own - zero reqwest/http/Url/hyper references. Its only two fs::write calls are at lines 154 and 205, both inside the #[cfg(test)] module that begins at line 147, so the production path READS and never writes. Needs no credential, never blocks (cold or expired cache means silence this session plus a detached refresh for the next), and is detection only - it never installs or updates anything."
    }
  ],
  "owners": [
    "CLI",
    "SECURITY"
  ],
  "last_semantic_audit": "2026-08-29"
}
