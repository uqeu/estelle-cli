# Estelle CLI scorecard

**Status:** public `v0.2.5` archive, installer, and npm bytes passed read-back on 2026-08-16. Source `v0.2.6`
first-run/provider onboarding is measured locally and awaits a public artifact. Rows record measurements,
not feature inventory.

| measured question | method / corpus | n | control | measured result | does not cover |
|---|---|---:|---|---|---|
| Does the current Estelle-owned TUI command surface satisfy its local contracts? | `RUST_MIN_STACK=8388608 cargo test --locked -p estelle-tui --bin estelle`; 2026-08-16 local source checkout | 236 binary tests | Typed fixtures, temp Git repositories, wiremock seams, renderer snapshots, five-way first-run state, provider-route mutants, and secret-safe receipts | 236 passed, 0 failed | Production provider inference, public v0.2.6 binary, or the much larger inherited TUI-library population |
| Does the typed client satisfy its local wire/storage contracts? | `RUST_MIN_STACK=8388608 cargo test --locked -p estelle-client`; 2026-08-16 local source checkout | 33 tests declared | Real secure-store/temp-file behavior, typed envelopes, cancellation, redaction, and exact provider-key request shape | 32 passed; 1 live-production test explicitly ignored | Production auth, entitlement, `/key` availability, or customer reachability |
| Does the wider locked TUI package pass with the required stack bound? | `RUST_MIN_STACK=8388608 cargo test --locked -p estelle-tui`; 2026-08-15 local source checkout | 3,542 executed passing tests across library, binary, and integration targets | The same physical-chord test overflows the default 2 MiB libtest stack; the named 8 MiB bound is the workflow contract | 3,542 passed; 5 tests requiring legacy Codex/tmux conditions ignored | A clean public checkout until the standalone parity repair is exercised there; any ignored PTY behavior |
| Does the native release build identify itself as the tagged product? | `cargo build --locked --release --package estelle-tui --bin estelle` then `./target/release/estelle --version`; macOS arm64, 2026-08-16 | 1 native artifact | Exact expected output `estelle 0.2.6`; warning-denied target builds remain CI-owned | Build passed; Mach-O arm64 and exact version matched | The other three targets, signature/provenance, public download, or clean-machine install |
| Does the shell installer enforce acquisition, command resolution, and first-screen contracts? | fixture and real-binary `scripts/test-installer.sh`, `scripts/test-release-package.sh`, `scripts/probe-first-run.py`; 2026-08-16 | 4 target selections · 2 PATH states · 2 shell profiles · 1 shadow mutant · 1 native 120x30 PTY · 4 reproducible archives | Exact resolved path/final version; consent-only profile change; printed export makes bare command resolve; older PATH binary fails with named path/remedy; ANSI-reconstructed first screen must contain `CONNECT ESTELLE` | PASS locally: fixture archive/control matrix passed; optimized arm64 `estelle 0.2.6` passed clean-PATH, shadow, exact-version, and real-PTY first-run gates; four archives reproduced | Interactive yes-response, other native target execution, public v0.2.6 bytes, signatures, or founder-machine state |
| Does the npm retirement shim install only a verified native artifact? | `npm test --prefix npm-shim`; packed tarball opened and enumerated, 2026-08-15 | 5 tests · 1 packed npm artifact | Four target mappings, HTTPS/GitHub redirect fence, exact checksum row, atomic install, checksum mutant; tarball must omit legacy modules | 5/5 passed; packed artifact contained only launcher, installer, package metadata, and README | npm trusted-publisher configuration, registry publication, GitHub release availability, or all archive-member mutants |
| Is fork provenance and the source-level egress register internally consistent? | `python3 scripts/check-fork-audit.py`; exact upstream object fetched in CI, 2026-08-16 | 1 upstream tree · 1 import tree · 11 reviewed high-risk blobs · 19 sink rows | Tree hashes, audited ancestry, exact risky path set/blob hashes (including untracked additions), source-symbol presence, and primitive occurrence counts | PASS; manifest `199366f…`; 14 released and 5 latent sinks matched the declared denominator | Runtime process-tree traffic, DNS destinations, provider behavior, or customer consent quality |
| Can the public repository pass its release pipeline from a clean checkout? | GitHub Actions run `31924677976`, immutable tag `v0.2.5`, clean runners, 2026-08-16 | 1 completed run · 1 validation · 4 target-native builds · release · npm publication | Tag/version, installer mutants, archive reproducibility, fork provenance, locked Rust tests, native architecture/version checks, release creation, attestation, and npm publication are fail-closed | SUCCESS: every validation, native build, release, provenance-signing, and npm job passed | Customer-byte readback is measured separately; founder machine state and interactive provider behavior |
| Can a customer acquire and identify every public native artifact? | GitHub release API/download, `shasum -a 256 -c`, `tar -tzf`, native execution, and public release installer; 2026-08-16 04:43Z | 9 public assets · 4 archives · 4 checksum rows · 1 selected native execution/install | Non-draft/non-prerelease `v0.2.5`; exact asset set; every archive hash and member set must match; selected archive must print the tag and install one destination binary | PASS for bytes/identity/install: 4/4 hashes and one-member sets matched; arm64 printed `estelle 0.2.5`; public installer and subsequent bare command on the probed PATH printed the same version. Local Sigstore verifier initialization failed, so independent attestation verification is unproved | Linux/x64 execution, interactive TUI, provider auth, founder default-shell restart, or independent provenance verification |
| Does the public npm package retire the legacy implementation and expose provenance? | isolated-cache `npm view --prefer-online @fatelabs/estelle`; 2026-08-16 04:43Z | 1 registry version · 1 integrity record · 1 attestation record | `latest` must equal `0.2.5`; registry integrity and SLSA v1 provenance metadata must exist | PASS: version `0.2.5`, sha512 integrity, and SLSA v1 attestation URL were returned | Fresh npm install/launch for v0.2.5, registry compromise, other targets, or future-version upgrade behavior |

## Publication gate — v0.2.5 PUBLIC BYTES PASS

The two public-distribution rows above read back all six required facts from public systems:

1. the remote tag resolves to the intended source commit;
2. four release archives and `SHA256SUMS` exist at customer URLs;
3. the downloaded archive hashes to its manifest row and contains exactly one regular binary;
4. that binary prints the tag version;
5. GitHub verifies its provenance attestation;
6. `npm view --prefer-online @fatelabs/estelle` reports the native compatibility shim, and `npm pack`
   shows no abandoned JavaScript implementation inside it.

The public `v0.2.5` bytes satisfy facts 1-4 and 6. The release workflow's provenance-signing step passed,
but the independent local verifier could not initialize, so fact 5 remains a read-back gap. Public installer
execution succeeded; the stronger default-shell restart and founder-machine proof remain outside this probe.
Source `v0.2.6` additionally gates shadowed commands and the real first-run picker before publication.
