# Estelle CLI scorecard

**Status:** public `v0.2.7` archive, installer, first-run, npm, and binary IP-boundary bytes passed read-back on 2026-08-16.
Provider acquisition is measured locally; provider inference remains absent. Rows record measurements, not
feature inventory.

| measured question | method / corpus | n | control | measured result | does not cover |
|---|---|---:|---|---|---|
| Does the current Estelle-owned TUI command surface satisfy its local contracts? | `RUST_MIN_STACK=8388608 cargo test --locked -p estelle-tui --bin estelle`; 2026-08-16 local source checkout | 236 binary tests | Typed fixtures, temp Git repositories, wiremock seams, renderer snapshots, five-way first-run state, provider-route mutants, and secret-safe receipts | 236 passed, 0 failed | Production provider inference, interactive public login completion, or the much larger inherited TUI-library population |
| Does the typed client satisfy its local wire/storage contracts? | `RUST_MIN_STACK=8388608 cargo test --locked -p estelle-client`; 2026-08-16 local source checkout | 33 tests declared | Real secure-store/temp-file behavior, typed envelopes, cancellation, redaction, and exact provider-key request shape | 32 passed; 1 live-production test explicitly ignored | Production auth, entitlement, `/key` availability, or customer reachability |
| Does the wider locked TUI package pass with the required stack bound? | `RUST_MIN_STACK=8388608 cargo test --locked -p estelle-tui`; 2026-08-16 local source checkout | 3,555 executed passing tests across library, binary, and integration targets | The same physical-chord test overflows the default 2 MiB libtest stack; the named 8 MiB bound is the workflow contract | 3,555 passed; 5 tests requiring legacy Codex/tmux conditions ignored | Any ignored PTY behavior |
| Does the native release build identify itself as the tagged product? | `cargo build --locked --release --package estelle-tui --bin estelle` then `./target/release/estelle --version`; macOS arm64, 2026-08-16 | 1 native artifact | Exact expected output `estelle 0.2.7`; warning-denied target builds remain CI-owned | Build passed; Mach-O arm64 and exact version matched | The other three targets, signature/provenance, public download, or clean-machine install |
| Does the shell installer enforce acquisition, command resolution, and first-screen contracts? | fixture and real-binary `scripts/test-installer.sh`, `scripts/test-release-package.sh`, `scripts/probe-first-run.py`; local plus public arm64 read-back, 2026-08-16 | 4 target selections · 2 PATH states · 2 shell profiles · 1 shadow mutant · 4 target-native CI probes · 1 public clean-home install · 4 reproducible archives | Exact resolved path/final version; consent-only profile change; printed export makes bare command resolve; older PATH binary fails with named path/remedy; ANSI-reconstructed first screen must contain `CONNECT ESTELLE` | PASS: fixture archive/control matrix passed; every native CI runner passed the installer/PTY gate; public arm64 `estelle 0.2.7` passed clean-PATH bare resolution and ran `estelle doctor` against `awesome-llm-apps` | Interactive yes-response, public execution of the other three targets, founder default-shell restart, or founder shadow state |
| Does the npm retirement shim install only a verified native artifact? | `npm test --prefix npm-shim`; packed tarball opened and enumerated, 2026-08-15 | 5 tests · 1 packed npm artifact | Four target mappings, HTTPS/GitHub redirect fence, exact checksum row, atomic install, checksum mutant; tarball must omit legacy modules | 5/5 passed; packed artifact contained only launcher, installer, package metadata, and README | npm trusted-publisher configuration, registry publication, GitHub release availability, or all archive-member mutants |
| Is fork provenance and the source-level egress register internally consistent? | `python3 scripts/check-fork-audit.py`; exact upstream object fetched in CI, 2026-08-16 | 1 upstream tree · 1 import tree · 11 reviewed high-risk blobs · 19 sink rows | Tree hashes, audited ancestry, exact risky path set/blob hashes (including untracked additions), source-symbol presence, and primitive occurrence counts | PASS; manifest `e527a9fe…`; 14 released and 5 latent sinks matched the declared denominator | Runtime process-tree traffic, DNS destinations, provider behavior, or customer consent quality |
| Does a release artifact preserve the server/CLI IP boundary? | `python3 scripts/test-ip-boundary.py` plus `python3 scripts/check-ip-boundary.py` on local, direct-download, shell-installed, and npm-installed arm64 artifacts; 2026-08-16 | 4 byte paths · 1 planted server-symbol mutant · 2 forbidden package prefixes · 4 target-native CI executions | Clean artifacts must pass; a payload containing `estelle.serve` must fail; every target-native release build must invoke the checker exactly once | PASS: every CI target crossed the gate; the 40,119,624-byte public/install artifact contained neither `estelle.serve` nor `estelle.agent`; the three customer paths shared SHA-256 `881ce252…`; the mutant failed | Symbols encoded in an unnamed form or proprietary logic with neither package prefix |
| Can the public repository pass its release pipeline from a clean checkout? | GitHub Actions run `31939611428`, immutable tag `v0.2.7`, clean runners, 2026-08-16 | 1 completed run · 1 validation · 4 target-native builds · release · npm publication | Tag/version, IP-boundary mutant and artifact gates, installer mutants and native first-run probes, archive reproducibility, fork provenance, locked Rust tests, native architecture/version checks, release creation, attestation, and npm publication are fail-closed | SUCCESS: every validation, native build, IP scan, release, provenance-signing, and npm job passed | Customer-byte readback is measured separately; founder machine state and interactive provider behavior |
| Can a customer acquire and identify every public native artifact? | remote ref and GitHub release API/download, `shasum -a 256 -c`, `tar -tzf`, native execution, public release installer, and clean-home `estelle doctor`; 2026-08-16 | 1 peeled tag/main match · 9 public assets · 4 archives · 4 checksum rows · 1 selected native execution/install | Non-draft/non-prerelease `v0.2.7`; exact asset set; every archive hash and member set must match; selected archive must print the tag and resolve as a bare command | PASS for bytes/identity/install: remote main and the peeled tag resolved to `854c4244…`; 4/4 hashes and one-member sets matched; direct and installed arm64 printed `estelle 0.2.7`; bare resolution passed against `awesome-llm-apps`. Local Sigstore verifier initialization failed, so independent attestation verification is unproved | Public execution of Linux/x64 artifacts, interactive login completion, founder default-shell restart, or independent provenance verification |
| Does the public npm package retire the legacy implementation and expose provenance? | isolated-cache `npm view`, `npm pack`, and fresh-prefix `npm install @fatelabs/estelle@0.2.7`; 2026-08-16 | 1 registry version · 1 integrity record · 1 attestation record · 1 public package install/launch | Registry version must equal `0.2.7`; integrity and SLSA v1 metadata must exist; tarball must contain only the compatibility shim; bare package command must launch native `0.2.7` | PASS: version `0.2.7`, sha512 integrity, and SLSA v1 attestation URL returned; public tarball contained only launcher, installer, metadata, and README; fresh install's bare command printed `estelle 0.2.7` and the embedded native hash matched the release | Registry compromise, non-arm64 install/launch, or future-version upgrade behavior |

## Publication gate — v0.2.7 PUBLIC BYTES PASS

The two public-distribution rows above read back all six required facts from public systems:

1. the remote tag resolves to the intended source commit;
2. four release archives and `SHA256SUMS` exist at customer URLs;
3. the downloaded archive hashes to its manifest row and contains exactly one regular binary;
4. that binary prints the tag version;
5. GitHub verifies its provenance attestation;
6. `npm view --prefer-online @fatelabs/estelle` reports the native compatibility shim, and `npm pack`
   shows no abandoned JavaScript implementation inside it.

The public `v0.2.7` bytes satisfy facts 1-4 and 6, including a fresh public npm install and native launch.
The release workflow's provenance-signing step passed, but the independent local verifier could not
initialize, so fact 5 remains a read-back gap. Public installer execution, bare-command resolution, and the
real first-run picker succeeded; the stronger default-shell restart and founder-machine proof remain outside
this probe.
